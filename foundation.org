#+TITLE: Zephyr Container Infrastructure: Foundation Engineering Design
#+AUTHOR: Sygaldry Project
#+DATE: 2026-02-16
#+STARTUP: overview
#+OPTIONS: toc:4 num:t H:6

* Preamble
:PROPERTIES:
:CUSTOM_ID: preamble
:END:

This document is the authoritative engineering design for the Zephyr container
infrastructure.  It describes every core subsystem at implementation depth:
data structures, control flow, error handling, file formats, and operational
procedures.  New features MUST be designed in accordance with these principles;
deviations require explicit justification and an update to this document.

This document is human-reviewed and updated by both agents and humans; it is
the source of truth for future design.

The eight foundational pillars, in dependency order:

1. *Spack Foundation Build Layer* -- immutable core packages (PyTorch, JAX, LLVM, Triton).
2. *Container Entrypoint Architecture* -- dual-persona execution for humans and agents.
3. *Snapshot and Registry Pipeline* -- bake validated environments into portable images.
4. *Spack Staging Mechanism* -- safely develop new foundation layers without disturbing production.
5. *Spack+UV Package Synergy* -- strict ownership boundary between Spack core and UV application packages.
6. *MLSys Application Images* -- HF, vLLM, sglang, TorchTitan, Megatron-LM, and beyond.
7. *Temporal Pipeline Orchestration* -- durable, resumable, observable multi-step ML pipelines.
8. *SAIL: Sygaldry Agentic Improvement Loop* -- autonomous issue discovery, planning, implementation, validation, landing, and self-analysis.

#+begin_quote
"Build heavy dependencies once with Spack; bake the validated environment into
snapshot images; layer application packages with UV on top of Spack without
ownership conflicts."
#+end_quote

Sygaldry combines Docker (isolation), Spack (HPC/scientific deps), Bazel
(incremental builds), uv (Python), and Temporal (workflow orchestration).  Key
benefits: hermetic builds, reproducibility, multi-layer caching, dynamic UID/GID
mapping, durable pipeline execution.

This document is the sole design source.  The following documents now exist as
stubs pointing here: =SYSTEM_DESIGN.md=, =container/ZEPHYR_SYSTEM_DESIGN.md=,
=temporal/TEMPORAL_DESIGN.md=, =temporal/TEMPORAL_ONBOARDING_GUIDE.md=,
=skills/zephyr/SKILL.md=, =skills/zephyr/VALIDATION.md=,
=skills/zephyr/portable/README.md=.  Agent instructions live in =CLAUDE.md= (not
a design document).

* System at a glance
:PROPERTIES:
:CUSTOM_ID: at-a-glance
:END:

** Layers

1. *Container* (Docker) — OS isolation, reproducible runtime, GPU access.
2. *Dependency* (Spack) — external scientific/HPC libraries, unified view at =/opt/spack_store/view=.
3. *Build* (Bazel) — fast incremental builds, sandboxing, multi-language.
4. *Python* (uv) — application packages on top of Spack, shared cache.
5. *Orchestration* (Temporal) — durable multi-step pipeline execution with dependency tracking.

** Dependency pinning

- *System*: Docker base image tags (e.g. =nvidia/cuda:12.9.1-cudnn-devel-ubuntu24.04=).
- *External*: =spack.lock= for exact Spack versions.
- *Application*: Bazel =WORKSPACE=, uv lockfiles.

** Data flow

Source (host) → Container → Spack view (=/opt/spack_store/view=) → Bazel/uv →
application artifacts → host output directory.

** Cache hierarchy

Docker layer cache → Spack binary cache → Bazel cache → uv cache.  Layout and
mounts: see *Host Storage Layout*.

* Pillar 1: Spack Foundation Build Layer
:PROPERTIES:
:CUSTOM_ID: spack-foundation
:END:

** Problem Statement

Machine learning workloads depend on a deep stack of CUDA-compiled native
libraries (PyTorch, JAX, Triton, LLVM, cuDNN, NCCL) whose compilation takes
3-8 hours and produces ~42 GB of artifacts.  These libraries have intricate ABI
coupling: a PyTorch built against CUDA 12.9 and cuDNN 9.10 must use exactly
those runtime versions, and JAX must share the same CUDA/cuDNN stack.  Pip
cannot manage this correctly because it treats each package as independent.

Spack solves this with unified concretization: a constraint solver that
guarantees every package in the environment uses exactly one version of every
shared dependency.

** Architecture

*** Layered Dependency Model

Docker Base Layer (=nvidia/cuda:12.9.1-cudnn-devel-ubuntu24.04=)
  CUDA 12.9.1 toolkit, cuDNN 9.10.2, NCCL
  Declared as Spack externals: these are shipped in the base image and are NOT built by Spack.

Spack Build Layer (=pkg/zephyr/spack_src.yaml=)
  py-torch 2.9 +cuda +distributed -magma
  py-jaxlib 0.7 +cuda
  py-triton 3.4
  llvm 20 +clang +lldb
  py-numpy, py-scipy
  python 3.13

*** Spack View

The Spack view at =/opt/spack_store/view= is a symlink tree that merges all
installed packages into a single prefix.  Downstream consumers (UV, entrypoints,
Bazel, application code) reference this path only -- never the install tree
hashes directly.

**** View options (spack.yaml)

- *path* (required) :: View root.  We use =/opt/spack_store/view= (simple form in =pkg/zephyr/spack_src.yaml=: =view: /opt/spack_store/view=).
- *link* :: =link= (default, symlinks) | =copy= | =hardlink=.  Symlink avoids duplication; copy/hardlink skip externals.  We use the default.
- *projection* :: Optional pattern (e.g. ={name}= or ={name}-{version}=) for layout under the view; default merges all packages into a single =bin/=, =lib/=, =lib/pythonX.Y/site-packages=, etc.
- *Unification* :: =concretizer: unify: true= ensures one concrete spec per package so the view can merge all install trees without path conflicts (see P1.1).
- *Regeneration* :: View is updated by =spack install= or explicitly via =spack env view regenerate=.

**** Container leverage of the view path

The container infrastructure uses =/opt/spack_store/view= as the single
integration point in these ways:

- *Single integration point* :: One prefix for =PATH= (=view/bin=), =PYTHONPATH= / =LD_LIBRARY_PATH= (=view/lib=, =view/lib/pythonX.Y/site-packages=).  Tools and runtimes need no per-package paths.
- *Fallback when activation fails* :: =container/entrypoints/default.sh= (=ensure_spack_view_fallback=), =run-job.sh=, =verify-gpu.sh= inject =view/bin=, view site-packages, and =view/lib= / =view/lib64= when =spack env activate= is unavailable (e.g. no repo, baked image only).
- *uv integration* :: =container/entrypoints/uv-install.sh= uses =SPACK_PY=/opt/spack_store/view/bin/python3=, creates venvs with =--system-site-packages= and a =spack-view.pth= pointing at the view's site-packages; =container/setup_user_environment.sh= =configure_uv_spack()= sets =PYTHONPATH= and =UV_FIND_LINKS= from the view when present.
- *Verification* :: =container/verify_uv_spack.py= and =verify-spack.sh= require core packages to resolve from the view; T8.2/T8.4 enforce "from view" and "no nvidia-* outside view."
- *Snapshot baking* :: View lives under =._view/<hash>/= on the host; =container/spack_snapshot.dockerfile= copies =._view= and creates =/opt/spack_store/view= → =._view/<hash>/=; =container/snapshot_spack.sh= checks =._view= exists before snapshot.

*** External CUDA Binding

CUDA and cuDNN are declared as non-buildable externals in =spack_src.yaml=:

#+begin_src yaml
packages:
  cuda:
    externals:
      - spec: cuda@12.9.1
        prefix: /usr/local/cuda
    buildable: false
  cudnn:
    externals:
      - spec: cudnn@9.10.2
        prefix: /usr/local/cuda
    buildable: false
#+end_src

This means Spack links against the Docker base image's CUDA but never compiles
it.  The Dockerfile ensures cuDNN headers and libraries are copied to
=/usr/local/cuda/include= and =/usr/local/cuda/lib64= so Spack's external
discovery finds them.

*** GPU Architecture Matrix

All CUDA-enabled packages specify =cuda_arch=61,75,80,86,89,90=:

| CUDA Arch | GPU Family              | Examples               |
|-----------+-------------------------+------------------------|
|        61 | Pascal                  | GTX 1080, Tesla P40    |
|        75 | Turing                  | RTX 2080, Tesla T4     |
|        80 | Ampere                  | A100, A30              |
|        86 | Ampere (consumer)       | RTX 3090               |
|        89 | Ada Lovelace            | RTX 4090, L40          |
|        90 | Hopper                  | H100, H200             |

This broad target set produces larger CUDA fatbinaries but guarantees a single
build serves any GPU from Pascal through Hopper without recompilation.

** Core Package Manifest

The canonical specification lives in =pkg/zephyr/spack_src.yaml=.

*** Deep Learning Frameworks

| Spack Spec                                       | Purpose                      |
|--------------------------------------------------+------------------------------|
| =py-torch@2.9.0+cuda cuda_arch=61,75,80,86,89,90=  | Core DL framework with CUDA  |
| =py-jax@0.7.0=                                     | Functional autodiff + XLA    |
| =py-jaxlib@0.7.0+cuda cuda_arch=61,75,80,86,89,90= | JAX CUDA runtime             |
| =py-torchvision@0.24.0=                            | Vision models and transforms |
| =py-torchaudio@2.9.0=                              | Audio I/O and transforms     |
| =py-triton@3.4.0=                                  | Custom GPU kernel compiler   |

PyTorch-specific package requirements:

#+begin_src yaml
py-torch:
  require:
    - "+cuda cuda_arch=61,75,80,86,89,90"
    - "+distributed"     # Enable torch.distributed (NCCL, Gloo)
    - "-magma"           # Disable MAGMA (compatibility issues, rarely needed)
#+end_src

*** Compiler Toolchain

| Spec                   | Purpose                             |
|------------------------+-------------------------------------|
| =llvm@20.1.8+clang+lldb= | LLVM/Clang compiler + LLDB debugger |
| =gdb@16.2=               | GNU debugger for native code        |

LLVM is in the foundation layer because Triton depends on it for kernel
compilation, and llvmlite/numba depend on it for JIT compilation.

*** Scientific Computing Stack

| Spec                  | Purpose                     |
|-----------------------+-----------------------------|
| =py-numpy@2.3.4=        | Fundamental array computing |
| =py-scipy@1.16.3=       | Scientific algorithms       |
| =py-numba=              | JIT compiler for Python     |
| =py-llvmlite=           | LLVM binding for numba      |
| =py-pandas@2.3.3=       | DataFrame operations        |

*** Concretizer Configuration

#+begin_src yaml
spack:
  concretizer:
    unify: true    # Single version per dependency across entire env
    reuse: false   # Do not reuse host microarch builds
  packages:
    all:
      require:
        - "target=x86_64"  # Generic target, no microarch tuning
#+end_src

- =unify: true= is critical.  Without it, PyTorch and JAX could each pull in
  different numpy/scipy versions, causing import conflicts.
- =reuse: false= prevents accidentally reusing a package compiled for
  skylake/haswell on a different microarchitecture.
- =target=x86_64= makes the entire store portable across any x86_64 machine.

** Build Script Control Flow

=pkg/zephyr/build.sh= implements the build with two distinct paths:

*** Lockfile Path (deterministic installs)

#+begin_example
spack.lock exists?
  ├── YES → Parse lock roots → Install each root by hash (/{hash})
  │         ├── Non-librosa roots: install unconditionally
  │         └── Librosa roots: guarded install with dry-run check
  │             ├── Dry-run touches torch/jax → skip librosa
  │             └── Dry-run clean → attempt install (non-fatal on failure)
  └── NO  → Concretize from spack_src.yaml → Dry-run safety check → Install
#+end_example

*** Forbidden Rebuild Guard

The guard is implemented as a regex match on Spack's install output:

#+begin_example
FORBIDDEN_REBUILD_PATTERN='(^|[^[:alnum:]_-])(py-torch|py-jax|py-jaxlib|torch|jax|jaxlib)([^[:alnum:]_-]|$)'
#+end_example

The =contains_forbidden_rebuilds()= function pipes any Spack dry-run output
through =rg= with this pattern.  If it matches, the build aborts immediately.
This guard runs in two places:

1. Before fresh concretization installs (no lockfile path).
2. Before librosa root installs (lockfile path, guarded policy).

*** Librosa Policy

Librosa is a heavyweight audio library whose transitive dependencies sometimes
conflict with the core torch/jax stack.  The build script implements a
three-mode policy controlled by =SYGALDRY_LIBROSA_POLICY=:

| Policy    | Behavior                                                         |
|-----------+------------------------------------------------------------------|
| =guarded= | Default.  Dry-run first; skip if torch/jax would be rebuilt.     |
| =remove=  | Never install librosa.                                           |
| =keep=    | Install without dry-run guard (may fail; non-fatal).             |

** Key Artifacts

| Artifact                      | Path                                    | Size    | Mutability   |
|-------------------------------+-----------------------------------------+---------+--------------|
| Spec template                 | =pkg/zephyr/spack_src.yaml=               | ~6 KB   | Editable     |
| Active env config             | =pkg/zephyr/spack.yaml=                   | ~6 KB   | Generated    |
| Pinned concretization         | =pkg/zephyr/spack.lock=                   | ~200 KB | Precious     |
| Build script                  | =pkg/zephyr/build.sh=                     | ~5 KB   | Editable     |
| Compiled packages             | =/opt/spack_store/install_tree=           | ~22 GB  | Append-only  |
| Spack database                | =/opt/spack_store/install_tree/.spack-db= | ~5 MB   | Auto-managed |
| Unified view                  | =/opt/spack_store/view=                   | ~400 MB | Regenerated  |
| Build staging area            | =/opt/spack_store/build_stage=            | ~5.5 GB | Ephemeral    |
| Source cache                  | =/opt/spack_store/source_cache=           | ~14 GB  | Cache        |
| Metadata cache                | =/opt/spack_store/misc_cache=             | ~700 KB | Cache        |
| Spack source (baked in image) | =/opt/spack_src=                          | ~150 MB | Immutable    |

** Design Principles

*** P1.1: Unified Concretization

All packages use =unify: true=.  This is non-negotiable.  Without it, Spack may
resolve py-torch against numpy 1.x while py-jax resolves against numpy 2.x,
producing silent import errors or segfaults.

*** P1.2: External CUDA Provider

CUDA and cuDNN are external.  Spack never builds them.  This saves hours of
compilation and guarantees the container's CUDA runtime matches the driver.

*** P1.3: Broad GPU Architecture Support

A single build covers Pascal through Hopper (6 architectures).  This trades
~20% larger fatbinary size for zero need to maintain per-GPU builds.

*** P1.4: Generic x86_64 Target

No host microarchitecture tuning (=target=x86_64=).  A Spack store built on an
AMD EPYC server works on an Intel Xeon workstation without recompilation.

*** P1.5: Lockfile Discipline

When =spack.lock= exists, the build script installs by hash (=/{hash}=) without
re-concretizing.  This guarantees bit-for-bit reproducibility.  The lockfile
is a precious artifact checked into the repository.

*** P1.6: Forbidden Rebuild Guard

If the resolver attempts to rebuild torch/jax/jaxlib, the build fails immediately.
These packages take 2-6 hours each.  The guard prevents operator error (e.g.,
bumping a spec that triggers a transitive rebuild of torch).

*** P1.7: View as Integration Surface

The Spack view at =/opt/spack_store/view= is the sole integration point for
downstream consumers.  UV, entrypoints, and application code all reference
the view -- never the install tree hashes directly.  See *** Spack View for
view options and container leverage.

** Operational Procedures

*** Building the Foundation Layer from Scratch

#+begin_example
# 1. Launch builder container with host Spack store mounted
SYGALDRY_BUILD_ROLE=builder ./container/launch_container.sh --entrypoint spack-build

# Build script handles:
#   - spack_src.yaml → spack.yaml
#   - spack concretize --force
#   - forbidden rebuild guard
#   - spack install
#   - spack env view regenerate
#   - Emits spack.lock for future deterministic builds
#+end_example

*** Adding a New Package to the Foundation

#+begin_example
# 1. Edit pkg/zephyr/spack_src.yaml: add the new spec
# 2. Use staging (Pillar 4) to evaluate safety
# 3. If staging passes: rebuild with updated spack_src.yaml
# 4. Commit updated spack.yaml + spack.lock
# 5. Rebuild snapshot (Pillar 3)
#+end_example

*** Upgrading a Core Package (e.g., PyTorch 2.9 → 2.10)

This is the most dangerous operation.  PyTorch upgrades can cascade rebuilds
across the entire dependency tree.

#+begin_example
# 1. Edit spack_src.yaml: bump py-torch version
# 2. Delete spack.lock (force fresh concretization)
# 3. Run staging: tools/zephyr_stage_spack.sh
#    - Staging will fail at forbidden guard if torch must rebuild (expected)
#    - Review concretize_report.json for the full rebuild set
# 4. If acceptable: run full build with builder role
# 5. Full GPU verification: verify-spack.sh, verify-gpu.sh
# 6. Commit new spack.lock + rebuild snapshot
#+end_example

* Pillar 2: Container Entrypoint Architecture
:PROPERTIES:
:CUSTOM_ID: entrypoints
:END:

** Problem Statement

The container must serve two radically different user personas:

1. *Human developers* need a rich interactive experience: welcome banners, GPU
   status, convenience aliases, environment auto-activation, and an intuitive
   bash shell.
2. *Coding agents and CI jobs* need a headless execution path: deterministic
   exit codes, no interactive prompts, minimal stdout noise, and structured
   logging for machine consumption.

Both personas must share the same Spack+CUDA initialization to avoid behavioral
divergence.

** Architecture

*** Entrypoint Lifecycle

Every container run goes through this lifecycle:

#+begin_example
┌─────────────────────────────────────────────────────────────────────────┐
│ Docker ENTRYPOINT: /opt/container_entrypoints/<name>.sh                │
│                                                                        │
│ Phase 1: Spack Initialization (shared across all entrypoints)          │
│   source /opt/spack_src/share/spack/setup-env.sh                       │
│                                                                        │
│ Phase 2: Spack Environment Activation (cascade)                        │
│   SYGALDRY_SPACK_ENV → /opt/spack_env/default → workspace/pkg/zephyr  │
│   Fallback: direct /opt/spack_store/view PATH/PYTHONPATH injection     │
│                                                                        │
│ Phase 3: CUDA Environment Setup                                        │
│   CUDA_HOME=/usr/local/cuda, PATH, LD_LIBRARY_PATH                     │
│                                                                        │
│ Phase 4: MLSys Venv Auto-Activation (if SYGALDRY_MLSYS_ENV set)       │
│   Single venv: source /opt/mlsys-envs/<env>/bin/activate               │
│   Multi-venv: find first sub-venv or use SYGALDRY_MLSYS_TARGET         │
│                                                                        │
│ Phase 5: Persona-Specific Dispatch                                     │
│   default.sh  → GPU validation → welcome banner → exec bash -i         │
│   run-job.sh  → exec "$@"                                              │
│   verify-*.sh → run checks → exit 0/1                                  │
│   uv-install  → build venv → exit 0/1                                  │
│   spack-*.sh  → builder role check → build → exit 0/1                  │
│   hf-download  → download → exit 0/1                                   │
└─────────────────────────────────────────────────────────────────────────┘
#+end_example

*** Spack Environment Activation Cascade

The cascade is the same in every entrypoint and tries locations in priority
order:

#+begin_example
1. $SYGALDRY_SPACK_ENV           (explicit override, e.g., staging env)
2. /opt/spack_env/default        (baked into snapshot image)
3. /opt/spack_env/zephyr         (legacy baked name)
4. $SYGALDRY_ROOT/pkg/zephyr     (workspace-mounted, legacy mode)
5. [FALLBACK] /opt/spack_store/view → inject into PATH/PYTHONPATH/LD_LIBRARY_PATH
#+end_example

The fallback (step 5) exists because in some edge cases (fresh image, missing
spack.lock) the formal Spack activation fails.  The view fallback ensures
torch/jax remain importable.

** Entrypoint Detailed Design

*** =default.sh= -- Human Interactive Shell

Control flow:

1. Spack init + cascade activation
2. View fallback if torch/jax not importable
3. *GPU fail-fast validation*: runs Python script that checks:
   - =torch.cuda.is_available()= and =torch.cuda.device_count() >= 1=
   - =jax.devices()= contains at least one GPU device
   - If either fails → print error to stderr → =exit 1=
4. MLSys venv auto-activation (=_activate_baked_mlsys_venv=)
5. Set convenience aliases:
   - =gpu-test= → quick PyTorch CUDA check
   - =jax-test= → quick JAX GPU check
   - =spack-build= → cd to env + run build.sh
   - =hf-dataset()= → quick dataset download function
6. If =$#= > 0: =exec "$@"= (non-interactive)
7. Otherwise: print welcome banner → =exec bash -i=

The welcome banner adapts to the image type:
- Base/snapshot: shows Spack commands, paths.
- MLSys image: shows env name, active venv, =mlsys-activate= hint.

*** =run-job.sh= -- Agent Headless Execution

Control flow:

1. =set -euo pipefail= (strict error handling)
2. Spack init + cascade activation
3. View fallback (no GPU validation -- job may be CPU-only setup)
4. MLSys venv auto-activation
5. =exec "$@"= -- replace the shell with the command

Key differences from =default.sh=:
- No GPU fail-fast (some jobs are setup steps that don't need GPU)
- No welcome banner
- No aliases
- The exit code of the user's command becomes the container's exit code

*** =spack-build.sh= / =spack-install.sh= -- Builder Role Entrypoints

#+begin_example
if SYGALDRY_BUILD_ROLE != "builder":
    echo "ERROR: Spack builds are restricted to the builder role." → exit 1
#+end_example

These entrypoints refuse to run unless the builder role is explicitly set.  This
prevents consumer containers from accidentally mutating the Spack store.

=spack-build.sh= resolves the build environment using the same cascade
(=SYGALDRY_SPACK_ENV= → =/opt/spack_env/default= → workspace) and then invokes
=./build.sh= in that directory.

*** =uv-install.sh= -- UV Venv Builder

Detailed in Pillar 5.  Core logic:

1. Find Spack Python (=/opt/spack_store/view/bin/python3=)
2. Read =spack_owned_packages.conf= → set =UV_NO_BUILD_ISOLATION_PACKAGE=
3. Locate =nvidia_overrides.txt=
4. =uv venv --python <spack-py> --system-site-packages $VENV_DIR=
5. Write =.pth= file for Spack view site-packages
6. Generate constraint file from Spack-owned package versions
7. =uv pip install --constraint <constraints> --override <nvidia-overrides> "$@"=

*** =verify-gpu.sh= -- GPU Hardware Validation

Reports:
- NVIDIA driver version, GPU name, memory (via =nvidia-smi=)
- CUDA toolkit version (via =nvcc=)
- =torch.cuda.is_available()=, device name, CUDA version, cuDNN version
- =jax.devices()= and GPU device count

Exit code 0 if both frameworks see GPU, non-zero otherwise.

*** =verify-spack.sh= -- Spack Environment Integrity

Four-step verification:

| Step | What it does                                                       |
|------+--------------------------------------------------------------------|
| 1    | Check =/opt/spack_store/view/bin= exists, torch/jax importable    |
| 2    | Import torch, check CUDA available, report device/version          |
| 3    | GPU tensor matmul: 100x100 matrix multiply on CUDA                |
| 4    | Neural network: 3-layer MLP forward + backward pass on GPU        |

The NN test exercises:
- =torch.nn.Linear=, =torch.nn.ReLU= on CUDA
- Forward pass with 32-sample batch
- =loss.backward()= to verify gradient computation
- Gradient existence check for all named parameters

JAX verification mirrors this:
- Matrix multiply on GPU
- 2-layer MLP with =jax.grad()= for gradient computation
- Shape assertions on all gradient tensors

*** =hf-download.sh= -- HuggingFace Asset Download

Modes:
- =dataset <id> [config] [split]= → =datasets.load_dataset()=
- =model <model_id>= → =huggingface_hub.snapshot_download()=

Creates a lightweight UV venv at =/opt/uv_cache/venvs/hf-download= with just
=datasets= and =huggingface-hub= (not the full Spack stack).  Downloads go to
=HF_HOME= (default =/opt/hf_cache=, shared across projects).

** Baked Entrypoints

All entrypoints are COPYd into the image at =/opt/container_entrypoints/=:

#+begin_example
# In dev_container.dockerfile:
COPY --chown=kvothe:kvothe container/entrypoints /opt/container_entrypoints
RUN chmod +x /opt/container_entrypoints/*.sh

# Image label signals baked status:
LABEL sygaldry.entrypoints.baked="true"
#+end_example

The launcher checks this label via =docker inspect= and resolves entrypoint
paths accordingly.  This means the container is fully self-contained --
no host-mounted scripts are needed.

** Design Principles

*** P2.1: Shared Initialization, Divergent Dispatch

All entrypoints share phases 1-4 (Spack, CUDA, MLSys venv).  They diverge only
at phase 5 (dispatch).  This prevents behavioral drift between interactive
and headless modes.

*** P2.2: GPU Fail-Fast (Interactive Only)

Only =default.sh= gates on GPU availability.  =run-job.sh= does not because
some agent jobs (file processing, dataset preparation) may legitimately run
without GPU.  The GPU check is a 3-second Python script, not a heavy operation.

*** P2.3: =exec= Semantics

All entrypoints use =exec= for the final command dispatch.  This replaces
the shell process with the target process, ensuring:
- Correct signal propagation (SIGTERM reaches the user's process)
- The container exit code equals the command's exit code
- No zombie shell process

*** P2.4: Builder Role is a Hard Gate

The builder role check (=SYGALDRY_BUILD_ROLE=builder=) is the first thing
=spack-build.sh= does.  If missing, it exits immediately.  This prevents
any consumer workflow from accidentally running =spack install=.

* Pillar 3: Snapshot and Registry Pipeline
:PROPERTIES:
:CUSTOM_ID: snapshot
:END:

** Problem Statement

The Spack store is 42 GB and takes hours to build.  It must be shared across
machines, CI runners, and developer workstations without requiring each to
run the build.  Docker images are the natural distribution mechanism, but
naively COPYing 42 GB into an image requires careful selection of what to
include and robust verification.

** Three-Stage Image Architecture

#+begin_example
Stage 1: Base Image (sygaldry/zephyr:base)                    ~15 GB
  FROM nvidia/cuda:12.9.1-cudnn-devel-ubuntu24.04
  ├── System packages: build-essential, cmake, ninja, git, etc.
  ├── Language runtimes: Python 3.13, Go 1.21.5, Bazel 6.4.0
  ├── Spack source: /opt/spack_src (v1.1.0, ~150 MB)
  ├── UV package manager: /usr/local/bin/uv
  ├── Baked env metadata: /opt/spack_env/default/spack_src.yaml
  ├── Baked entrypoints: /opt/container_entrypoints/*.sh
  ├── Override files: spack_owned_packages.conf, nvidia_overrides.txt
  └── User kvothe: dynamic UID/GID from build args

Stage 2: Spack Snapshot (sygaldry/zephyr:spack)               ~37 GB
  FROM sygaldry/zephyr:base
  ├── /opt/spack_store/install_tree (~22 GB)
  │     Compiled packages + .spack-db index
  ├── /opt/spack_store/._view (~400 MB)
  │     Unified view directory (symlinks to install_tree)
  ├── /opt/spack_store/view → ._view/<hash>/ (symlink)
  ├── /opt/spack_store/misc_cache (~700 KB)
  ├── /opt/spack_env/default/spack.yaml + spack.lock
  ├── Reindexed Spack DB: spack reindex
  ├── Labels: sygaldry.spack.baked=true
  └── EXCLUDED: build_stage (~5.5 GB), source_cache (~14 GB)

Stage 3: MLSys Images (sygaldry/zephyr:{vllm,sglang,...})     ~38-40 GB
  FROM sygaldry/zephyr:spack
  ├── /opt/mlsys-envs/<env>/ (UV venvs, 500 MB - 2 GB each)
  ├── Shell integration in /home/kvothe/.bashrc
  ├── Labels: sygaldry.mlsys.baked=true, sygaldry.mlsys.env=<name>
  └── ENV: SYGALDRY_MLSYS_VENV_ROOT, SYGALDRY_MLSYS_ENV
#+end_example

** Spack Snapshot Dockerfile (=spack_snapshot.dockerfile=)

The snapshot dockerfile uses BuildKit named contexts to access the host
Spack store without copying it into the build context:

#+begin_example
DOCKER_BUILDKIT=1 docker build \
  -f container/spack_snapshot.dockerfile \
  --build-context spack_store=/mnt/data_infra/.../spack_store \
  -t sygaldry/zephyr:spack .
#+end_example

Key operations in the dockerfile:

1. =COPY --from=spack_store install_tree /opt/spack_store/install_tree= (~22 GB)
2. =COPY --from=spack_store ._view /opt/spack_store/._view= (~400 MB)
3. =COPY --from=spack_store misc_cache /opt/spack_store/misc_cache= (~700 KB)
4. Create view symlink: =ln -sfn /opt/spack_store/._view/<hash>/ /opt/spack_store/view=
5. Create empty =build_stage= and =source_cache= (so Spack doesn't error)
6. Re-COPY =pkg/zephyr= as =/opt/spack_env/default= (with =spack.lock=)
7. Re-COPY entrypoints and override files
8. =spack reindex= to rebuild the database from copied install_tree
9. Set label =sygaldry.spack.baked=true=

=build_stage= (5.5 GB) and =source_cache= (14 GB) are intentionally excluded.
They are only needed during compilation and waste ~20 GB of image space.

** Build Script Control Flow (=snapshot_spack.sh=)

#+begin_example
1. VALIDATE prerequisites:
   ├── Base image exists (docker image inspect)
   ├── install_tree directory populated
   ├── ._view directory exists
   ├── spack.lock exists in pkg/zephyr/
   └── BuildKit available

2. REPORT sizes:
   ├── install_tree: du -sh
   ├── ._view: du -sh
   └── misc_cache: du -sh

3. BUILD:
   └── DOCKER_BUILDKIT=1 docker build ... (10-20 minutes for 22 GB COPY)

4. VERIFY (unless --no-verify):
   └── container/verify_snapshot.sh $SNAPSHOT_TAG
       Tests T1-T7 (see below)

5. PUSH (if --push):
   ├── Authenticate to registry (GITHUB_TOKEN for GHCR)
   ├── Tag: sygaldry/zephyr:spack → ghcr.io/phi9t/sygaldry/zephyr:spack
   ├── Tag: sygaldry/zephyr:spack → ghcr.io/phi9t/sygaldry/zephyr:spack-YYYYMMDD
   └── Push both tags
#+end_example

** Verification Test Suite (T1-T7)

Each test runs inside the snapshot image container.  No host mounts for Spack.

| ID | Name                     | Runner | What it checks                                      |
|----+--------------------------+--------+-----------------------------------------------------|
| T1 | Image Metadata           | No GPU | Label =sygaldry.spack.baked=true=                     |
|    |                          |        | ENV =SYGALDRY_SPACK_ENV=/opt/spack_env/default=       |
|    |                          |        | ENV =SYGALDRY_IN_CONTAINER=1=                         |
| T2 | Filesystem Structure     | No GPU | =/opt/spack_store/view= symlink resolves              |
|    |                          |        | =/opt/spack_store/view/bin= directory exists          |
|    |                          |        | =/opt/spack_env/default/spack.yaml= exists            |
|    |                          |        | =/opt/spack_env/default/spack.lock= exists            |
|    |                          |        | All 5 core entrypoints present and executable       |
|    |                          |        | =install_tree/.spack-db= exists                       |
| T3 | Python Imports           | No GPU | =import torch= + version (from view python)           |
|    |                          |        | =import jax= + version                                |
|    |                          |        | =import numpy= + version                              |
|    |                          |        | =import scipy= + version                              |
| T4 | Spack Env Activation     | No GPU | =spack env activate /opt/spack_env/default= succeeds  |
|    |                          |        | =spack find= shows >0 installed packages              |
| T5 | GPU Validation           | GPU    | =torch.cuda.is_available()= is True                   |
|    |                          |        | JAX sees >= 1 GPU device                            |
|    |                          |        | 64x64 tensor matmul on CUDA                         |
| T6 | Launcher Integration     | GPU    | Launcher + baked image: torch GPU check via run-job |
|    |                          |        | Launcher log contains "baked into image"            |
|    |                          |        | Launcher uses =/opt/container_entrypoints/= path      |
| T7 | No spack_store Shadowing | No GPU | =install_tree= is not empty (baked content visible)   |
|    |                          |        | =view/bin/python3= executable without host mount      |

** Launcher Integration with Baked Images

When the launcher detects =sygaldry.spack.baked=true=:

#+begin_example
spack_baked=$(docker inspect --format='{{index .Config.Labels "sygaldry.spack.baked"}}' $IMAGE)
if [ "$spack_baked" == "true" ]; then
    # SKIP: --volume=$HOST_SPACK_STORE:/opt/spack_store
    # Image has everything it needs
fi
#+end_example

The launcher also detects =sygaldry.entrypoints.baked=true= to resolve
entrypoint paths to =/opt/container_entrypoints/= rather than host-mounted paths.

** Registry Tag Strategy

| Tag Pattern                           | Mutability | Purpose                      |
|---------------------------------------+------------+------------------------------|
| =zephyr:spack=                        | Mutable    | Latest known-good snapshot   |
| =zephyr:spack-YYYYMMDD=              | Immutable  | Date-stamped for rollback    |
| =zephyr:vllm=                        | Mutable    | Latest vLLM image            |
| =zephyr:vllm-YYYYMMDD=              | Immutable  | Date-stamped                 |

Immutable date tags are never overwritten.  This enables deterministic rollback
by changing the image tag in deployment configs.

** Design Principles

*** P3.1: Exclude Ephemeral Build Artifacts

=build_stage= and =source_cache= are excluded from the snapshot.  They save
~20 GB of image space and are only needed during compilation.

*** P3.2: Reindex After COPY

=spack reindex= is mandatory after COPYing the install tree.  The database
(.spack-db) contains absolute paths and timestamps that may be stale after
the copy.  Reindexing rebuilds the database from the actual installed files.

*** P3.3: View Symlink, Not Copy

The view is a symlink tree (=/opt/spack_store/._view/<hash>/=) with the
top-level symlink =/opt/spack_store/view= pointing to it.  The snapshot
dockerfile recreates this symlink dynamically.

*** P3.4: Verification Before Promotion

No image is pushed to the registry until it passes T1-T7.  The =--no-verify=
flag exists for development iteration only.

* Pillar 4: Spack Staging Mechanism
:PROPERTIES:
:CUSTOM_ID: staging
:END:

** Problem Statement

Adding or upgrading a Spack spec can trigger cascading rebuilds that destroy
hours of compilation work.  Operators need a way to evaluate changes safely
before committing them to the production environment.

** Architecture

*** Two-Tool Pipeline

The staging mechanism consists of two tools that work in sequence:

**** Tool 1: =zephyr_concretize_analyze.py= -- Analysis Only

This tool concretizes in an isolated staging directory and reports which
hashes are missing from the installed tree.  It NEVER runs =spack install=.

Input: source env directory + optional additional specs.
Output: JSON report with concretized nodes, missing nodes, forbidden matches.

#+begin_example
Arguments:
  --source-env-dir    Source env (default: /workspace/pkg/zephyr)
  --stage-root        Base dir for staging (default: /tmp/zephyr_concretize_staging)
  --spec              Additional spec to add (repeatable)
  --forbidden-regex   Regex for packages that must not be missing
  --output-json       Path for JSON report
  --keep-stage        Retain staging directory
  --allow-deprecated  Allow deprecated package versions
#+end_example

Control flow:

#+begin_example
1. Copy source env files to staging directory:
   ├── spack.yaml (or spack_src.yaml → spack.yaml)
   └── spack.lock (if exists)
2. If --spec provided: spack add <spec> in staging env
3. spack concretize --force [--deprecated]
4. For each root spec: spack spec -j <spec> → parse JSON nodes
5. spack find --json → get installed hashes
6. Compute: missing = concretized - installed
7. Flag forbidden: regex match on missing package names
8. Emit JSON report
#+end_example

JSON report structure:

#+begin_src json
{
  "source_env_dir": "/workspace/pkg/zephyr",
  "stage_dir": "/tmp/.../run-XXXXXX",
  "concretization_success": true,
  "forbidden_regex": "(py-torch|py-jax|py-jaxlib|torch|jax|jaxlib|llvm)",
  "totals": {
    "concretized_nodes": 347,
    "installed_nodes_matched": 342,
    "missing_nodes": 5,
    "forbidden_missing_nodes": 0
  },
  "missing": [
    {
      "name": "py-soundfile",
      "version": "0.10.3.post1",
      "hash": "abc123...",
      "forbidden_match": false,
      "origin_specs": ["py-soundfile@0.10.3.post1"]
    }
  ]
}
#+end_src

**** Tool 2: =zephyr_stage_spack.sh= -- Analysis + Guarded Install

This tool wraps the analyzer and conditionally proceeds to install.

#+begin_example
Input:
  --source-yaml     Source manifest (default: /workspace/pkg/zephyr/spack_src.yaml)
  --spec            Spec to analyze (repeatable)
  --python-import   Module to import for verification (repeatable)
  --forbidden-names CSV of forbidden package names

Output directory: outputs/spack_stage/<run_id>/
  ├── source_env/       Copy of source manifest
  ├── analyze_stage/    Concretization staging directory
  └── logs/
      ├── concretize_report.json
      ├── new_requirements.txt
      ├── final_status.json
      ├── install.log
      ├── verify_pkg_zephyr.log
      ├── verify_gpu.log
      └── verify_imports.log
#+end_example

Control flow:

#+begin_example
1. Copy source YAML to staging
2. Run zephyr_concretize_analyze.sh → concretize_report.json
3. Extract staging env path from report
4. Generate new_requirements.txt from missing packages
5. FORBIDDEN CORE GUARD:
   ┌──────────────────────────────────────────────────────────┐
   │ Parse missing list from report.                          │
   │ Filter by forbidden names (default: py-torch, py-jax,   │
   │   py-jaxlib, torch, jax, jaxlib, llvm).                 │
   │ If ANY forbidden package is missing → exit 10.           │
   │ Production environment remains untouched.                │
   └──────────────────────────────────────────────────────────┘
6. spack install in staging env → install.log
7. spack env view regenerate → append to install.log
8. Verify (if --no-gpu-verify not set):
   ├── pkg/zephyr/verify.sh --require-gpu → verify_pkg_zephyr.log
   └── verify-gpu.sh entrypoint → verify_gpu.log
9. Verify explicit Python imports (if --python-import):
   └── Python import test → verify_imports.log
10. Write final_status.json: ok or failed + step + message
#+end_example

** Isolation Boundaries

| Resource           | Production Env    | Staging Env                    | Mutation Risk |
|--------------------+-------------------+--------------------------------+---------------|
| =spack.yaml=         | =pkg/zephyr/=       | =outputs/spack_stage/<id>/=      | None (copied) |
| =spack.lock=         | =pkg/zephyr/=       | Generated in staging           | None          |
| Install tree       | =/opt/spack_store/= | Shared (new pkgs install here) | Additive only |
| Source/build cache | =/opt/spack_store/= | Shared                         | Additive only |
| View               | Production view   | Staging-specific view          | None          |
| Logs               | N/A               | =<run_id>/logs/=                 | None          |

The install tree is intentionally shared.  Staging installs NEW packages into
the same tree that production uses.  This is safe because Spack packages are
content-addressed (each package lives at a unique hash path).  Installing
new packages does not modify existing ones.

** Exit Codes

| Code | Meaning                                     |
|------+---------------------------------------------|
|    0 | Staging succeeded, all verifications passed |
|    2 | Concretization analysis failed              |
|    3 | Failed to generate new requirements list    |
|   10 | Forbidden core guard triggered              |
|   11 | =spack install= failed                        |
|   12 | =spack env view regenerate= failed            |
|   13 | =pkg/zephyr/verify.sh= failed                 |
|   14 | =verify-gpu.sh= failed                        |
|   15 | Explicit Python import verification failed  |

** Design Principles

*** P4.1: Read-Only Production Files

Staging never modifies production =spack.yaml= or =spack.lock=.  All changes
happen in an isolated directory under =outputs/spack_stage/=.

*** P4.2: Forbidden Core Guard Exits Hard

Exit code 10 is a clear signal: the operator's proposed change requires
rebuilding a core package.  This is not an error to retry -- it requires a
conscious decision to proceed with a full build.

*** P4.3: Shared Install Tree is Safe

Content-addressed storage means new packages get their own hash directories.
Existing packages are never modified by new installs.  The only risk is disk
space consumption.

*** P4.4: Machine-Readable Reports

All artifacts are JSON or line-delimited text.  =final_status.json= provides a
single-file summary that agents can parse to determine pass/fail without
reading logs.

* Pillar 5: Spack+UV Package Synergy
:PROPERTIES:
:CUSTOM_ID: spack-uv-synergy
:END:

** Problem Statement

Users need to install Python packages (transformers, vllm, sglang, datasets)
that depend on PyTorch and NumPy.  If UV (the fast Python package installer)
is allowed to resolve these dependencies freely, it will:

1. Pull in pip copies of =torch=, =numpy=, =triton= that shadow the Spack versions.
2. Pull in =nvidia-cublas-cu12=, =nvidia-cudnn-cu12=, etc. that conflict with
   the Docker base image's CUDA.
3. Result in undefined-symbol crashes, CUDA version mismatches, and subtle
   numerical differences.

The synergy layer ensures UV can install application packages while treating
the Spack-provided packages as immutable system dependencies.

** Three-Tier Package Ownership Model

#+begin_example
┌────────────────────────────────────────────────────────────────────────┐
│ Tier 3: UV Application Packages                                       │
│   transformers, tokenizers, accelerate, vllm, sglang, datasets        │
│   Installed into /tmp/test-venv or /opt/mlsys-envs/<env>              │
│   Authority: per-project requirements.txt or YAML env definition      │
├────────────────────────────────────────────────────────────────────────┤
│ Tier 2: Spack Core ML/Scientific Packages (IMMUTABLE at UV time)      │
│   torch, torchvision, torchaudio, jax, jaxlib, triton                 │
│   numpy, scipy, scikit-learn, numba, llvmlite                         │
│   matplotlib, pandas, soundfile, jupyterlab                           │
│   Authority: container/spack_owned_packages.conf                      │
├────────────────────────────────────────────────────────────────────────┤
│ Tier 1: Docker CUDA/NVIDIA Runtime (FORBIDDEN to UV)                  │
│   libcuda.so, libcudnn.so, libnccl.so                                 │
│   nvidia-cublas-cu12, nvidia-cudnn-cu12, etc. (pip names)             │
│   Authority: Docker base image + container/nvidia_overrides.txt       │
└────────────────────────────────────────────────────────────────────────┘
#+end_example

** Enforcement Mechanisms

*** Mechanism 1: Build Isolation Bypass (=UV_NO_BUILD_ISOLATION_PACKAGE=)

The environment variable is populated from =spack_owned_packages.conf=:

#+begin_example
UV_NO_BUILD_ISOLATION_PACKAGE="torch,torchvision,torchaudio,jax,jaxlib,triton,
  numpy,scipy,scikit-learn,numba,llvmlite,matplotlib,pandas,soundfile,
  jupyterlab,transformer-engine-torch"
#+end_example

This tells UV: "Do not create an isolated build environment for these packages.
They are already available as system packages."  Without this, UV would try to
download and compile PyTorch from source if a dependency lists it as a build
requirement.

*** Mechanism 2: Spack Constraint File (generated at install time)

=uv-install.sh= generates a constraint file by querying Spack-owned packages:

#+begin_src python
import importlib.metadata as md
spack_owned = {"torch", "numpy", "scipy", ...}  # from spack_owned_packages.conf
for dist in md.distributions():
    name = dist.metadata.get("Name")
    if name and name.lower() in spack_owned:
        version = re.sub(r'\+.*$', '', dist.version)  # strip local tags
        print(f"{name}=={version}")
#+end_src

Output example:

#+begin_example
torch==2.9.0
torchvision==0.24.0
numpy==2.3.4
scipy==1.16.3
#+end_example

This file is passed as =--constraint= to =uv pip install=, which forces UV to
accept these exact versions rather than resolving newer ones.

The local-version-tag stripping (=re.sub(r'\+.*$', '', version)=) is necessary
because Spack sometimes appends build hashes (e.g., =2.9.0+eaa9e4e=) that
UV's PEP 440 parser rejects.

*** Mechanism 3: NVIDIA Override File (=nvidia_overrides.txt=)

Uses the "impossible marker" pattern to make nvidia pip packages unresolvable:

#+begin_example
nvidia-cublas-cu12; sys_platform == 'never'
nvidia-cudnn-cu12; sys_platform == 'never'
nvidia-nccl-cu12; sys_platform == 'never'
# ... 15+ nvidia-* packages for CUDA 11 and 12
#+end_example

=sys_platform == 'never'= is never true, so UV treats these packages as
if they don't exist.  When vllm or sglang declare =nvidia-cublas-cu12= as a
dependency, UV silently skips it.

Also includes workarounds:

#+begin_example
# Fix broken Spack dist-info metadata
python-dateutil>=2.8.2     # Spack reports 0.0.0, actual is 2.8.2

# Relax tight numpy pin from megatron-core
numpy>=2.0.0               # megatron pins <2.0.0, Spack has 2.3.4
#+end_example

*** Mechanism 4: LLM Serving Overrides (=llm_serving_overrides.txt=)

Opt-in override file for packages like vLLM and sglang that pin exact versions:

#+begin_example
torchvision>=0.20.0     # vLLM pins exact torchvision per release
torchaudio>=2.9.0       # sglang pins exact torchaudio
numba>=0.60.0           # vLLM pins numba==0.61.2
triton>=3.4.0           # PyPI metadata says 3.5.0, Spack has 3.4.0
soundfile>=0.10.0       # sglang pins 0.13.1, Spack has 0.10.3.post1
torch>=2.9.0; sys_platform == 'never'   # Block pip torch entirely
#+end_example

The =torch; sys_platform == 'never'= line is the nuclear option: it
prevents UV from resolving pip-distributed torch under any circumstances,
forcing the system to use the Spack-compiled version.

** UV Venv Creation (=uv-install.sh=) Detailed Flow

#+begin_example
1. FIND Spack Python:
   /opt/spack_store/view/bin/python3 (preferred)
   $(command -v python3) (fallback)

2. READ spack_owned_packages.conf:
   /opt/container_entrypoints/spack_owned_packages.conf (preferred)
   Hardcoded fallback list (if file not found)

3. SET UV_NO_BUILD_ISOLATION_PACKAGE from conf file

4. LOCATE nvidia_overrides.txt:
   /opt/container_entrypoints/nvidia_overrides.txt (preferred)
   Relative to entrypoints directory (fallback)

5. CREATE venv:
   uv venv --python $SPACK_PY --system-site-packages $VENV_DIR

6. INJECT .pth file:
   echo "/opt/spack_store/view/lib/python3.13/site-packages" > \
     $VENV_DIR/lib/python3.13/site-packages/spack-view.pth

7. GENERATE constraint file:
   $SPACK_PY -c "...enumerate spack-owned versions..." > /tmp/spack-constraints.txt

8. INSTALL:
   uv pip install \
     --constraint /tmp/spack-constraints.txt \
     --override nvidia_overrides.txt \
     [--override $UV_EXTRA_OVERRIDES] \
     "$@"
#+end_example

The =.pth= file in step 6 is critical.  =--system-site-packages= alone makes
the system Python's site-packages visible, but the Spack view's site-packages
are at a different path than the system Python.  The =.pth= file explicitly
adds the Spack view path to =sys.path=.

** Verification (T8.1-T8.8)

=container/verify_uv_layering.sh= validates the entire synergy:

| Test | What it does                                                         | Blocking? |
|------+----------------------------------------------------------------------+-----------|
| T8.1 | Create UV venv, install transformers + tokenizers + accelerate       | Yes       |
| T8.2 | Verify torch, torchvision, torchaudio, jax, numpy, scipy, triton    | Yes       |
|      | all resolve from =/opt/spack_store/view= (not pip)                   |           |
| T8.3 | Verify transformers, tokenizers, accelerate from UV venv (not Spack) | Yes       |
| T8.4 | No =nvidia-*= packages installed outside Spack view                  | Yes       |
| T8.5 | torch CUDA matmul + HF tokenizer load after UV layering              | Yes (GPU) |
| T8.6 | vLLM: install, provenance, no nvidia pip, ABI safety                 | Serving   |
| T8.7 | datasets: install in separate venv                                   | Non-block |
| T8.8 | sglang: install, provenance, no nvidia pip, ABI safety               | Serving   |

ABI safety checks (T8.6, T8.8) detect hard-fail patterns:
- =undefined symbol= -- wrong .so version loaded
- =libnvJitLink= -- CUDA JIT linker mismatch
- =libcusparse= -- cuSPARSE version mismatch

** Design Principles

*** P5.1: Spack Packages Are Immutable at UV Time

UV never installs, upgrades, or rebuilds Spack-owned packages.  This is
enforced by three independent mechanisms that provide defense-in-depth.

*** P5.2: NVIDIA Pip Packages Are Forbidden

Mixing pip-distributed CUDA libraries with the Docker base image's CUDA
causes crashes.  The override file makes them unresolvable.

*** P5.3: System Site-Packages + .pth

The combination of =--system-site-packages= and the =.pth= file creates a
transparent bridge: UV venvs can "see" all Spack packages without copying
them.  This means =import torch= in any UV venv resolves to the Spack-compiled
version.

*** P5.4: Constraint File Uses Spack-Owned Packages Only

The constraint file is generated ONLY from packages listed in
=spack_owned_packages.conf=, not from all packages in the Spack view.  This
prevents stale transitive dependencies (e.g., =datasets==0.0.9= from a broken
dist-info) from poisoning the UV resolver.

*** P5.5: Override Files Are Composable

The base =nvidia_overrides.txt= is always applied.  Additional overrides
(=llm_serving_overrides.txt=) are opt-in via =UV_EXTRA_OVERRIDES=.  This
allows different environments to have different relaxation policies.

* Pillar 6: MLSys Application Images
:PROPERTIES:
:CUSTOM_ID: mlsys-images
:END:

** Problem Statement

Two distinct use cases require baked MLSys images:

1. *First-party Sygaldry images*: Named catalog images (=zephyr:vllm=,
   =zephyr:sglang=, =zephyr:hf=, etc.) built once, pushed to a registry, and
   reused across projects.  Each image has one or more UV venvs layered on top
   of the Spack snapshot.

2. *Repo-scoped project images*: An external ML project wants a baked image
   containing its own specific Python dependencies, without waiting for a
   first-party image to be published.  The project vendors a minimal build kit
   (=.zephyr-mlsys/=) and runs =just build= to produce a project-specific image
   with its venv baked in.

Both tracks share the same Spack base, constraint system, and UV layering
strategy.

** Architecture

*** Image Build Pipeline

#+begin_example
┌───────────────────────────────────────────────────┐
│  zephyr:base  (~15 GB)                            │
│  CUDA 12.9.1, Ubuntu 24.04, OS toolchain          │
└──────────────────────┬────────────────────────────┘
                       │
┌──────────────────────▼────────────────────────────┐
│  zephyr:spack  (~37 GB)                           │
│  torch 2.9, JAX 0.7, LLVM 20, triton 3.4, ...    │
│  Baked at /opt/container_entrypoints/:            │
│    uv-install.sh, spack_owned_packages.conf,      │
│    nvidia_overrides.txt, llm_serving_overrides.txt │
└───────┬───────────────────────┬───────────────────┘
        │ (first-party catalog) │ (repo-scoped)
┌───────▼──────┐        ┌───────▼────────────────────────┐
│ zephyr:vllm  │        │  project-A/mlsys:latest (+1-3GB)│
│ zephyr:sglang│        │  Built by: build-mlsys.sh       │
│ zephyr:hf    │        │  UV venv: /opt/repo-venv        │
│ ...          │        │  ENTRYPOINT: mlsys-entrypoint.sh│
└──────────────┘        └───────────┬────────────────────┘
                                    │ (image chaining)
                        ┌───────────▼────────────────────┐
                        │  project-B/mlsys:latest        │
                        │  FROM project-A/mlsys:latest   │
                        │  Adds own apt pkgs + UV deps   │
                        └────────────────────────────────┘
#+end_example

*** Key Invariant: Constraint Files in Spack Snapshot

=uv-install.sh= and the three constraint files are baked into =zephyr:spack= at
=/opt/container_entrypoints/=.  Every MLSys image (first-party or repo-scoped)
inherits them through Docker layer inheritance.  No re-vendoring is needed.

The vendor kit's =container/= directory only supplies the two new in-container
scripts (=entrypoint.sh=, =run-job.sh=) that are not in the Spack base:

#+begin_example
/opt/container_entrypoints/   (in Spack base image -- inherited)
  uv-install.sh
  spack_owned_packages.conf
  nvidia_overrides.txt
  llm_serving_overrides.txt

/opt/mlsys-entrypoint.sh      (in repo-scoped image -- COPYed from vendor kit)
/opt/mlsys-run-job.sh         (in repo-scoped image -- COPYed from vendor kit)
#+end_example

** Repo-Scoped Images (Vendor Kit)

*** Overview

External repos vendor a minimal build kit into =.zephyr-mlsys/=.  The kit is
self-contained: it includes the build scripts, config template, and the two
in-container scripts that get COPYed into the built image.

#+begin_example
.zephyr-mlsys/               # vendored into external repo
  config.yaml                # user-edited: base image + deps
  image.lock                 # written by build-mlsys.sh after build
  justfile                   # generated: just build / run / check
  bin/
    build-mlsys.sh           # bash orchestration (~140 lines)
    _build_helper.py         # Python: config parse + Dockerfile gen
    launch-mlsys.sh          # docker run wrapper
  container/
    entrypoint.sh            # COPYed into image as /opt/mlsys-entrypoint.sh
    run-job.sh               # COPYed into image as /opt/mlsys-run-job.sh
#+end_example

Install or update the vendor kit with:

#+begin_example
zephyr_mlsys_vendor.sh install --target-repo /path/to/my-project
zephyr_mlsys_vendor.sh update  --target-repo /path/to/my-project
zephyr_mlsys_vendor.sh check   --target-repo /path/to/my-project
#+end_example

The =install= subcommand auto-detects =pyproject.toml= in the target repo and
pre-fills =python_deps.pyproject= in the generated =config.yaml=.  It also
generates a =justfile= with convenience recipes.

*** Config Schema (=zephyr-mlsys/v1=)

#+begin_src yaml
apiVersion: zephyr-mlsys/v1

# Base image. Can be a Spack snapshot OR an existing mlsys image (chaining).
# Tip: pin with @sha256:DIGEST for reproducible builds.
base_image: sygaldry/zephyr:spack@sha256:<digest>

# Output image name
image: my-project/mlsys:latest

# Optional: extra apt packages
# apt_packages:
#   - libsndfile1-dev
#   - ffmpeg

# Python dependencies -- choose ONE of the three options:
python_deps:
  # Option A: inline list
  packages:
    - "transformers>=4.50"
    - accelerate

  # Option B: requirements file
  # requirements: requirements-mlsys.txt

  # Option C: read from pyproject.toml [project.dependencies]
  # pyproject: pyproject.toml
  # extras: [mlsys]   # optional: [project.optional-dependencies.mlsys]

# Venv path inside container (default: /opt/repo-venv)
# venv_path: /opt/repo-venv
#+end_src

*** Build Tooling

**** =build-mlsys.sh= (bash orchestration, ~140 lines)

#+begin_example
.zephyr-mlsys/bin/build-mlsys.sh [--config <path>] [--force-rebuild] [--no-cache]
#+end_example

Responsibilities:
1. Parses CLI flags
2. Creates a temp build context dir
3. Calls =_build_helper.py= to generate =Dockerfile= and copy container scripts
4. Reads =IMAGE_NAME= from =_build_helper.py= stdout (KEY=VALUE protocol)
5. Runs =docker buildx build --load -t <image> <ctx>=
6. Retrieves digest, writes =image.lock=

Skips rebuild if =image.lock= exists and image is present locally (override
with =--force-rebuild=).

**** =_build_helper.py= (Python, ~340 lines)

Called by =build-mlsys.sh= with four arguments:
=<config.yaml> <build_ctx_dir> <repo_root> <kit_root>=

Responsibilities:
- Parses =config.yaml= with a stdlib-only minimal YAML parser (no PyYAML dep)
- Resolves Python deps from three sources:
  - =packages:= inline list
  - =requirements:= file path (relative to repo root)
  - =pyproject:= toml path + optional =extras:= list (uses =tomllib= stdlib,
    Python 3.11+; falls back to =tomli= if available)
- Copies =container/entrypoint.sh= and =container/run-job.sh= into build context
- Generates =Dockerfile= in build context
- Prints =IMAGE_NAME=<value>= to stdout; diagnostic output to stderr
- Warns (does not error) if =base_image= lacks =@sha256:= digest

**** =launch-mlsys.sh=

#+begin_example
.zephyr-mlsys/bin/launch-mlsys.sh [--ro] [--no-gpu] [--] <command...>
#+end_example

Reads =image.lock=, runs the baked image with the repo mounted at =/repo=.

| Flag       | Effect                                          |
|------------+-------------------------------------------------|
| =--ro=     | Mount repo read-only inside container           |
| =--no-gpu= | Skip =--runtime=nvidia --gpus=all= docker flags |

Legacy env var =MLSYS_DISABLE_GPU=1= is still accepted.

*** Generated Dockerfile

=_build_helper.py= generates this pattern:

#+begin_example
FROM <base_image>

ARG BUILT_AT=unknown
ARG REPO_SOURCE_REV=unknown

LABEL sygaldry.mlsys.repo_image="true"
LABEL sygaldry.mlsys.built_at="${BUILT_AT}"
LABEL sygaldry.mlsys.base_image_ref="<base_image>"
LABEL sygaldry.mlsys.repo_source_rev="${REPO_SOURCE_REV}"

# Optional apt packages
RUN apt-get update && apt-get install -y --no-install-recommends \
    <pkg> ... \
    && rm -rf /var/lib/apt/lists/*

# UV venv on top of Spack (uv-install.sh + constraint files inherited from base)
RUN --mount=type=cache,target=/opt/uv_cache \
    VENV_DIR=/opt/repo-venv \
    UV_CACHE_DIR=/opt/uv_cache \
    /opt/container_entrypoints/uv-install.sh \
    <pkg1> <pkg2> ...

# In-container scripts (from vendor kit container/ directory)
COPY container/entrypoint.sh /opt/mlsys-entrypoint.sh
COPY container/run-job.sh    /opt/mlsys-run-job.sh
RUN chmod +x /opt/mlsys-entrypoint.sh /opt/mlsys-run-job.sh

ENV VIRTUAL_ENV=/opt/repo-venv
ENV PATH=/opt/repo-venv/bin:${PATH}
ENV MLSYS_VENV_PATH=/opt/repo-venv

LABEL sygaldry.mlsys.baked="true"
LABEL sygaldry.mlsys.venv_path="/opt/repo-venv"

ENTRYPOINT ["/opt/mlsys-entrypoint.sh"]
#+end_example

*** In-Container Scripts

**** =entrypoint.sh= → =/opt/mlsys-entrypoint.sh=

Docker ENTRYPOINT baked into every repo-scoped image (~30 lines).

Execution sequence:
1. Activate venv at =$MLSYS_VENV_PATH= (default =/opt/repo-venv=)
2. Extend =PATH= and =LD_LIBRARY_PATH= from Spack view (=/opt/spack_store/view=)
3. Set =CUDA_HOME=/usr/local/cuda=, extend =PATH= and =LD_LIBRARY_PATH=
4. =exec "$@"= -- replace shell with user command

**** =run-job.sh= → =/opt/mlsys-run-job.sh=

In-container job runner (~72 lines).

#+begin_example
/opt/mlsys-run-job.sh [--log <job-name>] [--] <command...>
#+end_example

- Sets =SYGALDRY_IN_CONTAINER=1=
- Activates venv
- With =--log <job-name>=: tees stdout+stderr to =/repo/.zephyr-mlsys/logs/<job>.log=
- Without =--log=: =exec "$@"= directly

** First-Party Image Catalog

First-party images are built by =snapshot_mlsys.sh= for the Sygaldry registry:

#+begin_example
./container/snapshot_mlsys.sh vllm              # Build one
./container/snapshot_mlsys.sh all               # Build all envs
./container/snapshot_mlsys.sh vllm --push       # Build + verify + push
./container/snapshot_mlsys.sh vllm --dry-run    # Show commands only
#+end_example

| Env Name           | Docker Tag    | Key Packages                         |
|--------------------+---------------+--------------------------------------|
| =hf-transformers=  | =hf=          | transformers, tokenizers, accelerate |
| =vllm=             | =vllm=        | vllm>=0.15.0                         |
| =sglang=           | =sglang=      | sglang>=0.5.8, fastapi, uvicorn      |
| =torchtitan=       | =torchtitan=  | tensorboard, tomli-w, datasets>=2.0  |
| =megatronlm=       | =megatronlm=  | megatron-core, transformer-engine    |
| =llm-serving-all=  | =mlsys=       | all of vllm + sglang + hf (3 venvs)  |

The =zephyr:mlsys= all-in-one image hosts three venvs because vLLM and sglang
have conflicting transitive dependencies (flashinfer, ray, xgrammar):

#+begin_example
/opt/mlsys-envs/llm-serving-all/
├── hf/      transformers + tokenizers + accelerate
├── vllm/    vLLM (with llm_serving_overrides)
└── sglang/  sglang + API deps (with llm_serving_overrides)

Interactive: mlsys-activate vllm
Agent:       SYGALDRY_MLSYS_TARGET=vllm
#+end_example

** Design Principles

*** P6.1: Snapshot as Sole Base

Every MLSys image (first-party or repo-scoped) uses =sygaldry/zephyr:spack=
(or a previously built MLSys image for chaining) as its =FROM=.  No MLSys image
may run =spack install=.  They only add UV venvs.

*** P6.2: No GPU at Build Time

Docker builds have no GPU.  Validation happens post-build.  This allows
building on CPU-only CI and validating on GPU nodes.

*** P6.3: Multi-Venv for Conflicting Packages

When packages have incompatible transitive dependency graphs (vLLM vs sglang),
create separate venvs rather than fighting the resolver.

*** P6.4: Hard-Fail ABI Detection

Patterns like =undefined symbol= and =libnvJitLink= in import errors indicate
a fundamental ABI mismatch between the UV-installed package and the Spack core.
These are treated as hard failures that block image promotion.

*** P6.5: Broken Dist-Info Patching

The dockerfile patches =python-dateutil= dist-info from 0.0.0 to 2.8.2.  This
is a known Spack issue where the dist-info metadata does not match the actual
installed version.  Without this patch, UV refuses to resolve any package
that requires =python-dateutil>=2.8.2=.

*** P6.6: Repo-Scoped Images via Vendor Kit

Sygaldry exposes its UV+Spack layering capability to external repos through a
vendored build kit.  The kit is small (~5 files) and self-contained.  The
target repo never needs a Sygaldry clone; only the vendored =.zephyr-mlsys/=
directory is required.  The vendor script handles install, update, and
migration from the legacy =.codex-zephyr-mlsys/= directory name.

*** P6.7: Image Chaining

A repo-scoped MLSys image can itself be used as the =base_image= for a second
image.  The second image inherits the Spack layer and the first image's venv
and adds its own apt packages and UV deps on top.  Digest-pinning is
recommended but not required; an unpinned =base_image= emits a warning.

*** P6.8: Split Build Logic (bash + Python)

=build-mlsys.sh= is ~140 lines of pure bash (CLI, docker invocation, lock
write).  All YAML/TOML parsing and Dockerfile generation live in
=_build_helper.py=, which is independently testable without running Docker.

* Pillar 7: Temporal Pipeline Orchestration
:PROPERTIES:
:CUSTOM_ID: temporal-orchestration
:END:

** P7 Problem Statement

Multi-step ML pipelines (download model → build image → run GPU job → evaluate)
need durable, resumable, observable execution.  Shell scripts break on failure
with no restart capability; cron lacks dependency tracking; ad-hoc runners
provide no visibility into in-flight state.  Temporal provides exactly-once
activity semantics, persistent workflow state, automatic retry with backoff, and
a rich inspection API.

** P7.1 Architecture

#+begin_example
plan.yaml → cmd/orchestrate → Temporal workflow (pipeline.go) → worker activities (steps.go)
                                        ↓                               ↓
                                  log artifacts              GPU containers / HF / Docker
                                        ↓
                              CLI inspection / web visualizer
#+end_example

Components:

- =cmd/orchestrate= — CLI with =run=, =validate=, =status= subcommands; pre-submit plan
  validation; plan manifest emit on run start.
- =cmd/worker= — registers 2 workflows + 8 activities; polls task queue; configures
  retry policies and activity options.
- =internal/workflows/pipeline.go= — streaming DAG scheduler; selector-based async
  dispatch; skip-logic evaluation; step output collection.
- =internal/activities/steps.go= — execution layer; structured JSONL log writers;
  stdout output capture; per-step log files.
- =scripts/logs_cli.py= — run inspection CLI (list, summary, show-steps, dag, tail).
- =visualizer/server.js= — Node.js web UI (port 8787) with DAG reconstruction.

** P7.2 YAML Plan Schema

Top-level fields:

#+begin_example
log_dir: <string>           # Log artifact root (overrides TEMPORAL_LOG_DIR)
params: { key: value, ... } # Plan-level parameters (all strings)
env: { KEY: VALUE, ... }    # Environment variables for all steps
imports: [path1.yaml, ...]  # Template files to import
templates: { name: step_def, ... }  # Inline template definitions
steps: [...]                # Required; ≥1 step
#+end_example

Step common fields:

#+begin_example
id: <string>                # Required; unique within plan
name: <string>              # Optional; defaults to id
type: <string>              # One of 8 step types (or use template:)
template: <string>          # Reference to template (alternative to type)
depends_on: [step-id, ...]  # Step IDs that must complete before this step
when:
  step: <string>            # Required; step ID whose outcome to check
  status: success|failure   # Required; trigger condition
command: <string>           # For command type; direct shell command
args: [...]                 # Argument list
env: { KEY: VALUE, ... }    # Per-step environment overrides
working_dir: <string>       # Working directory for step execution
timeout_seconds: <int>      # Per-step timeout; ≥1
allow_failure: bool         # If true, downstream steps are not cascaded-skipped
retry:
  max_attempts: <int>            # ≥1; number of total attempts
  initial_interval_seconds: <int>
  backoff_coefficient: <float>   # ≥1.0
  maximum_interval_seconds: <int>
#+end_example

Type-specific config blocks (one required per step):

| Type | Config Block | Required Fields | Optional Fields |
|------+--------------+-----------------+-----------------|
| =command= | (inline) | =command= | =args=, =env=, =working_dir= |
| =download= | =download:= | =url=, =output= | =sha256= |
| =docker_build= | =docker_build:= | =image= | =context=, =dockerfile=, =build_args=, =labels=, =platform=, =target= |
| =docker_push= | =docker_push:= | =image= | — |
| =package_build= | =package_build:= | =command= | =args=, =env=, =working_dir= |
| =container_job= | =container_job:= | =command= | =project_id=, =entrypoint=, =env=, =gpu=, =launcher_path= |
| =hf_download_dataset= | =hf_download_dataset:= | =dataset_id= | =config=, =split=, =cache_dir= |
| =hf_download_model= | =hf_download_model:= | =model_id= | =cache_dir= |

** P7.3 Workflow Execution Model

Streaming DAG scheduling — no pre-computation of full schedule:

1. Main loop: find steps whose =depends_on= are all completed (=depsCompleted= check).
2. Launch each ready step via Temporal selector; selector blocks awaiting first completion.
3. Record outcome (success/failure/skip) and step outputs.
4. Loop back to re-evaluate all pending steps against updated state.

=shouldSkip(step)= logic (evaluated before dispatch):

1. If step has =when= clause: skip unless the referenced step's actual status matches =when.status=.
2. If any =depends_on= step failed and does not have =allow_failure=: cascade-skip this step.
3. Otherwise: execute.

Activity defaults:

| Parameter | Default | Notes |
|-----------+---------+-------|
| StartToCloseTimeout | 2 hours | Per-attempt wall-clock limit |
| InitialInterval | 5 seconds | First retry backoff |
| BackoffCoefficient | 2.0 | Exponential growth factor |
| MaximumInterval | 60 seconds | Retry backoff cap |
| MaximumAttempts | 3 | Total attempts before failure |

Per-step =retry= block overrides non-zero fields only; zero values retain the default.

** P7.4 Variable Interpolation

All string fields in a plan support ==${{ ... }}== expressions.  Resolution is
reflection-based and recursive (a resolved value may itself contain expressions).

Available namespaces:

| Expression | Source |
|------------+--------|
| =${{ params.KEY }}= | Plan-level =params= map |
| =${{ env.KEY }}= | Environment variable (=os.Getenv=) |
| =${{ steps.ID.outputs.KEY }}= | Output from upstream step (see below) |

Output capture protocol: a step's stdout is scanned line-by-line for the marker:

#+begin_example
::set-output name=<key>::<value>
#+end_example

Key must match =[A-Za-z_][A-Za-z0-9_-]*=.  One marker per line.  Outputs are
parsed before the activity returns and stored in the workflow state for downstream
interpolation.

** P7.5 Template System

Load order: =imports= files first (in order) → inline =templates= map → per-step
fields (override).

Merge strategy: non-zero/non-empty step fields override corresponding template
fields.  Zero-value fields (empty string, 0, false, nil slice) do not override.

Templates cannot reference other templates.  The =depends_on= field is always
taken from the step definition, never the template.

** P7.6 Environment Variable Configuration

| Variable | Default | Purpose |
|----------+---------+---------|
| =TEMPORAL_ADDRESS= | =localhost:7233= | Temporal server gRPC address |
| =TEMPORAL_NAMESPACE= | =default= | Workflow namespace |
| =TEMPORAL_TASK_QUEUE= | =orchestration= | Worker task queue name |
| =TEMPORAL_LOG_DIR= | =./logs= | Log artifact root directory |
| =TEMPORAL_LOG_MAX_BYTES= | =10000= | Max bytes for stdout/stderr in activity payloads |

** P7.7 Log and Artifact Infrastructure

Default root: =$TEMPORAL_LOG_DIR= (fallback chain: user =-log-dir= arg →
=$TEMPORAL_LOG_DIR= env var → =./logs= → =/tmp=).

Per-run artifacts written under the log root:

| File | Description |
|------+-------------|
| =events.jsonl= | =step_started= / =step_finished= lifecycle event stream |
| =<workflow>_<run>_plan.json= | Plan manifest emitted at submit time |
| =<workflow>_<run>_<step>_stdout.log= | Full stdout text for the step |
| =<workflow>_<run>_<step>_stderr.log= | Full stderr text for the step |
| =<workflow>_<run>_<step>_structured.jsonl= | Line-oriented structured stream with =timestamp=, =stream= (stdout/stderr), =partial= flag |

** P7.8 CLI Contract

#+begin_example
# Execute a plan (synchronous by default)
go run ./cmd/orchestrate run      -plan <plan.yaml> [-async] [-output yaml|json] [-set k=v ...]

# Validate a plan without running it
go run ./cmd/orchestrate validate -plan <plan.yaml>

# Query workflow status
go run ./cmd/orchestrate status   -workflow-id <id> [-run-id <id>] [-output yaml|json]
#+end_example

Pre-submit validation checks:
- Required fields present by step type (e.g., =url=+output for download, =model_id= for hf_download_model)
- All =depends_on= references resolve to existing step IDs
- All =when.step= references resolve to existing step IDs
- No dependency cycles (DFS with white/gray/black node coloring)

** P7.9 Inspection and Observability

CLI (=scripts/logs_cli.py=):

#+begin_example
./scripts/logs_cli.py list-runs                      # All workflow runs
./scripts/logs_cli.py summary --latest               # Summary of most recent run
./scripts/logs_cli.py show-steps --latest            # Step-by-step status table
./scripts/logs_cli.py logs --latest                  # All step logs
./scripts/logs_cli.py dag --latest                   # ASCII DAG visualization
./scripts/logs_cli.py tail --follow                  # Live event stream
./scripts/logs_cli.py show-steps --workflow-id <id> --run-id <run>
./scripts/logs_cli.py follow --workflow-id <id> --run-id <run>
#+end_example

Web visualizer (=visualizer/server.js=, port 8787):

| Endpoint | Description |
|----------+-------------|
| =GET /api/runs= | All runs with status summary |
| =GET /api/runs/:id= | Run details and step outcomes |
| =GET /api/dag?runId=...= | DAG reconstruction from manifest + events |
| =GET /api/events= | Raw event stream |

Temporal Web UI available at =localhost:8233= (temporal CLI dev server) or
=localhost:8080= (Docker Compose).

** P7.10 Developer Workflow

Quickstart (three terminals):

#+begin_example
# Terminal 1: Start Temporal dev server
cd temporal && ./scripts/start-temporal.sh   # UI at localhost:8233

# Terminal 2: Start worker
TEMPORAL_ADDRESS=localhost:7233 TEMPORAL_NAMESPACE=default \
  TEMPORAL_TASK_QUEUE=orchestration go run ./cmd/worker

# Terminal 3: Run a plan
go run ./cmd/orchestrate run -plan examples/quickstart/01_hello.yaml
#+end_example

One-command launcher: =scripts/run.sh <plan.yaml>= — auto-starts Temporal if needed.

Testing ladder:

#+begin_example
cd temporal
go vet ./...
go test ./...                       # 89 unit tests
./scripts/test-e2e.sh               # Smoke test (PR gate)
./scripts/e2e/run_medium.sh         # Medium E2E (nightly)
./scripts/e2e/run_heavy.sh          # Heavy E2E with GPU (nightly)
#+end_example

Adding a new step type (checklist):

1. Add execution logic in =internal/activities/steps.go=
2. Register activity in =cmd/worker/main.go=
3. Add dispatch case in =internal/workflows/pipeline.go=
4. Add validation in =cmd/orchestrate/validate.go=
5. Update JSON schema (if maintained)
6. Write an example plan under =examples/=
7. Add E2E coverage in =scripts/e2e/=
8. Update this section (Pillar 7)

** P7.11 Known Gaps (as of 2026-03-05)

- =cmd/worker/main.go= has no direct unit test coverage (entry point only)
- Visualizer (=visualizer/server.js=) has no auth guard — local/dev use only
- Log retention/cleanup policy is not implemented; manual or cron cleanup expected
- No built-in plan parameter validation beyond type checks (e.g., URL format)

* Pillar 8: SAIL — Sygaldry Agentic Improvement Loop
:PROPERTIES:
:CUSTOM_ID: sail
:END:

** Overview

SAIL is an autonomous repository-improvement loop built on top of Pillar 7
(Temporal).  It discovers actionable issues, deterministically generates a
focused fix plan, asks an agent to implement the change, validates the result,
and lands the fix when the quality gate passes.  The default landing mode is
=direct=: successful runs commit on =main= and push =origin/main=.  A
=pull_request= mode still exists, but it is optional rather than the default.

The outer loop lives in =tools/agentic/run_improvement_loop.sh=.  It discovers
issues, filters and deduplicates them, runs one Temporal workflow per issue,
records durable run artifacts, and can retry bounded failures with failure
context from the previous attempt.  The unattended wrapper in
=tools/agentic/sail_cron.sh= adds clean-checkout enforcement, worker management,
run analysis, and one bounded self-improvement pass for SAIL itself.

Primary entry points:
- =sygaldry sail= or =just sail= — run one interactive/manual SAIL pass.
- =sygaldry sail cron= or =just sail-cron= — run the unattended wrapper once.
- =sygaldry sail discover= — preview discovered issues only.
- =sygaldry sail analyze-run --run-dir ...= — analyze one persisted run.

Operator reference: =skills/agentic-improvement/SKILL.md=.

** P8.1 Architecture

#+begin_example
sygaldry sail
  ├─ preflight: nc -z $TEMPORAL_HOST $TEMPORAL_PORT
  ├─ discover_issues.py  →  JSON[]{id, priority, type, title, description, files}
  ├─ filter: min_priority, max_tasks_per_run
  └─ for each issue (sequential; max_parallel=1 on one checkout):
       dedup: skip if .agentic/attempted.jsonl already records it as handled
       dedup: in PR mode, also skip if a matching remote branch already exists
       choose branch_name:
         direct mode      → main
         pull_request mode → agentic/<timestamp>-<slug>
       for attempt in 1..max_retries:
         go run ./cmd/orchestrate -plan improvement_loop.yaml -set key=value ...
           1. plan           command   generate_plan.py
           2. branch         git_op    op=branch
           3. implement      agent_task implementer_engine/model
           4. verify_changes command   fail fast if agent changed nothing
           5. validate       command   ./validate_all.sh --quick
           6. commit_pr      git_op    op=commit      (only on validate success)
           7. push_branch    git_op    op=push        (only on validate success)
           8. create_pr      git_op    op=create-pr   (PR mode only; direct mode
                                                       treats this as landed)
           9. rollback       git_op    op=reset       (on validate failure)
         persist: run.json, discovery_stats.json, issue_attempts.jsonl, logs
         retry: pass prior validate failure context into the next plan
       append terminal result to .agentic/attempted.jsonl

sygaldry sail cron
  ├─ require clean main checkout, fast-forward origin/main
  ├─ ensure Temporal server + managed worker
  ├─ run primary SAIL pass
  ├─ analyze persisted artifacts with analyze_sail_run.py
  └─ if synthetic SAIL issues were emitted: run exactly one self-fix pass
#+end_example

** P8.2 Workflow Step Types

The per-issue Temporal workflow uses three step families:

| Step type | Used for | Notes |
|-----------+----------+-------|
| =command= | planning, no-change detection, validation | Runs local commands such as =generate_plan.py= and =./validate_all.sh --quick=. |
| =agent_task= | implementation | Dispatches an agent CLI subprocess and captures stdout/stderr plus structured logs. |
| =git_op= | branch, commit, push, PR, rollback | Wraps =tools/agentic/git_ops.sh= for repository mutations. |

*** =agent_task=

Dispatches to an agent CLI subprocess and collects stdout/stderr as Temporal
activity output.  Supported engines today are =claude=, =codex=, =cursor=, and
=echo= for testing.  Prompt files use =${{ params.KEY }}= templates, and params
are also injected as =SAIL_PARAM_<KEY>= environment variables into the
subprocess.

*** =git_op=

Wraps =tools/agentic/git_ops.sh=, which implements six operations:

| Operation | Effect |
|-----------|--------|
| =branch= | Prepare the target branch for the workflow (=main= in direct mode, =agentic/...= in PR mode) |
| =commit= | =git add -A; git commit= (exits 1 if nothing staged) |
| =push= | =git push origin <branch>= |
| =create-pr= | Open a PR in PR mode, or report a landed change in direct mode |
| =reset= | Save diff patch to =/tmp/=, then =git reset --hard HEAD; git clean -fd= |
| =delete-branch= | =git push origin --delete <branch>= |

** P8.3 Issue Discovery

=discover_issues.py= aggregates seven issue sources:

| Source | Priority | Tool |
|--------|----------|------|
| Go test failures | 1 (critical) | =go test ./... -json= |
| Go vet warnings | 2 (high) | =go vet ./...= |
| shellcheck findings | 2-3 | =shellcheck -f json= |
| TODO/FIXME/HACK comments | 2-3 | =git grep= |
| ruff lint findings | 3 (normal) | =ruff check --output-format json= |
| foundation.org drift | 3 (normal) | file-existence check |
| Uncovered Go functions | 3 (normal) | =go test -coverprofile + go tool cover -func= |

Issues are sorted by priority then ID.  Deduplication hash: SHA-1 of
=<type-specific payload>=.  Filter via =--min-priority=, cap each source with
=--max-per-type=, and persist per-source timing in =discovery_stats.json= during
run-loop execution.

** P8.4 Planning and Implementation Roles

Planning is local and deterministic by default.  The =plan= step runs
=tools/agentic/generate_plan.py=, which turns one discovered issue plus optional
failure context into:
- a focused plan file for the implementer step
- a normalized =task_title= that is safe to use in commit subjects
- a narrow, issue-type-specific remediation strategy

Implementation remains agent-driven.  The =implement= step reads the plan file
from the planner output and executes the change in the repository working tree.
The default implementer engine is configured in =tools/agentic/config.yaml=, but
it can be overridden per run with environment variables such as
=SAIL_IMPLEMENTER_ENGINE= and =SAIL_IMPLEMENTER_MODEL=.

On retry, the previous validation failure excerpt is passed back into the next
planning attempt through =failure_context_file=.  This keeps retries bounded and
focused on the last observed failure rather than re-running a blind first attempt.

Commit and PR titles are normalized through the planner output, yielding subjects
such as =agentic(todo): resolve fixme in path/to/file=.

** P8.5 Operating SAIL

#+begin_example
# Manual run
sygaldry sail
just sail

# Preview only
sygaldry sail discover
sygaldry sail --dry-run
just sail-discover
just sail-dry

# Unattended wrapper
sygaldry sail cron
just sail-cron
just sail-initial MAX_TASKS=1

# Services and analysis
sygaldry sail worker
sygaldry sail validate-plan
sygaldry sail analyze-run --run-dir /path/to/run
just sail-analyze RUN_DIR=/path/to/run
#+end_example

Current defaults in =tools/agentic/config.yaml=:
- planner engine: =local=
- implementer engine/model: =claude= / =claude-sonnet-4-6=
- landing mode: =direct=
- max tasks per run: =3=
- max retries per task: =2=
- min priority: =2=

** P8.6 Artifacts and Self-Improvement

SAIL keeps two layers of durable state:

1. =.agentic/attempted.jsonl= in the repo root records issue-level terminal
   outcomes such as =landed=, =pr_created=, or =skipped=.  This prevents the
   outer loop from repeating already handled work.
2. Persisted run artifacts capture everything about one invocation.  The cron
   wrapper writes these under its runtime root (default:
   =/mnt/data_infra/zephyr_container_infra/sygaldry/logs/sail=), with one run
   directory per invocation containing files such as:
   - =run.json=
   - =discovered_issues.json=
   - =discovery_stats.json=
   - =issue_attempts.jsonl=
   - per-attempt workflow logs and structured agent-session logs

=tools/agentic/analyze_sail_run.py= turns those artifacts into:
- normalized =agent_sessions.jsonl=
- normalized =workflow_events.jsonl=
- =summary.json= and =summary.md=
- synthetic SAIL-only issues when the loop itself needs work

Synthetic self-improvement issues currently cover categories such as:
=discovery_slow=, =planner_failure=, =implementer_failure=, =empty_commit=,
=pr_creation_failure=, and =retry_loop=.

The cron wrapper uses that analyzer to optionally launch exactly one follow-on
self-fix pass after the primary pass.  That makes SAIL recursive in a bounded
way: it can improve its own prompts, orchestration, or runtime behavior without
falling into an unbounded loop.

** P8.7 Safety Guarantees

1. *Validation gate* — =./validate_all.sh --quick= must exit 0 before commit and push.
2. *Meaningful commit titles* — the planner emits a normalized =task_title=, and the
   pipeline uses =agentic(<issue_type>): <task_title>= for landed commits.
3. *Empty-commit guard* — =git_op commit= exits non-zero when nothing is staged,
   preventing false-success workflows.
4. *Rollback with patch capture* — failed validation triggers a reset, and the
   discarded diff is preserved for post-mortem analysis.
5. *Deduplication* — =.agentic/attempted.jsonl= prevents repeated handling of
   issues already landed, skipped, or otherwise marked terminal.
6. *Clean unattended execution* — =sail_cron.sh= refuses dirty or off-=main=
   execution and fast-forwards =main= before starting a run.
7. *Bounded compute* — =max_tasks_per_run=, =max_retries_per_task=, and a single
   self-fix pass cap unattended cost and churn.
8. *Selectable landing mode* — SAIL can operate either in direct-land mode
   (=main=) or PR mode (=agentic/...= branches), with direct landing as the
   current default.

** P8.8 Key Files

| File | Role |
|------|------|
| =bin/sygaldry= | CLI entrypoints for =sail=, =discover=, =worker=, =cron=, =analyze-run=, and =validate-plan= |
| =justfile= | Convenience wrappers such as =just sail=, =just sail-cron=, and =just sail-initial= |
| =tools/agentic/config.yaml= | Planner/implementer selection, loop limits, repo landing mode |
| =tools/agentic/improvement_loop.yaml= | Temporal YAML pipeline for one issue workflow (9 logical steps) |
| =tools/agentic/run_improvement_loop.sh= | Outer loop: discovery, dedup, retries, orchestration, artifact persistence |
| =tools/agentic/generate_plan.py= | Deterministic planner that emits =plan_file= and =task_title= |
| =tools/agentic/discover_issues.py= | Multi-source issue scanner with discovery timing telemetry |
| =tools/agentic/sail_cron.sh= | Unattended wrapper: clean checkout, worker management, primary + self-fix runs |
| =tools/agentic/analyze_sail_run.py= | Run analyzer that emits summaries, normalized logs, and synthetic SAIL issues |
| =tools/agentic/git_ops.sh= | Git operations backend for =git_op= activity |
| =tools/agentic/prompts/implementer.md= | Implementer system prompt |
| =tools/agentic/prompts/retry.md= | Retry system prompt |
| =temporal/internal/activities/agent_task.go= | =AgentTask= activity implementation |
| =temporal/internal/activities/git_op.go= | =GitOp= activity implementation |
| =temporal/internal/workflows/pipeline.go= | Workflow engine that executes the declarative step graph |
| =skills/agentic-improvement/SKILL.md= | Full operator reference |

* Verification Infrastructure
:PROPERTIES:
:CUSTOM_ID: verification
:END:

** Layered Test Architecture

Tests are organized by cost (time, prerequisites) into 9 layers:

| Layer | Script(s)                | Prerequisites        | Time   | Coverage                                |
|-------+--------------------------+----------------------+--------+-----------------------------------------|
| L0    | =bash -n=, =shellcheck=      | None                 | ~5s    | Shell syntax, linting                   |
| L1    | =launch_container_test.sh= | None                 | ~3s    | Launcher arg parsing, config resolution |
| L2    | =go test ./...=            | Go                   | ~10s   | Temporal orchestration (89 tests)       |
| L3    | =ruff=, =black=, =pytest=      | =.venv-lint=           | ~5s    | Python style, formatting, unit tests    |
| L4    | =verify_multi_repo.sh=     | Docker               | ~30s   | Mount layout, env vars, store integrity |
| L5    | =verify_snapshot.sh=       | Docker + snapshot    | ~60s   | Baked image (T1-T7)                     |
| L6    | =verify_uv_layering.sh=    | Docker + snapshot    | ~120s  | UV+Spack layering (T8.1-T8.8)           |
| L7    | =verify_multi_repo_e2e.sh= | Docker + GPU         | ~120s  | GPU from external repo                  |
| L8    | =--infra-full= flag        | Docker + GPU + Spack | ~30min | Full infra: build, install, validate    |

** CI Orchestrator (=validate_all.sh=)

Selectively runs layers based on flags:

| Flag                 | Layers         | Typical Use                        |
|----------------------+----------------+------------------------------------|
| (none)               | L0 + L2 + L3   | Developer pre-commit               |
| =--quick=              | L2 + L3        | Fast lint, skip shellcheck         |
| =--multi-repo-unit=    | L1             | Launcher unit tests only           |
| =--multi-repo=         | L1 + L4 + L7   | Full multi-repo integration        |
| =--snapshot=           | L5             | After building snapshot image      |
| =--snapshot-no-gpu=    | L5 (T1-T4,T7)  | CI without GPU hardware            |
| =--uv-layering=        | L6             | After UV layering changes          |
| =--uv-layering-no-gpu= | L6 (T8.1-T8.4) | CI without GPU                     |
| =--infra=              | L8 (smoke)     | Preflight + image verify           |
| =--infra-full=         | L8 (full)      | Everything: build + install + test |

** Release Gates

| Image Type       | Required Tests              | Blocking |
|------------------+-----------------------------+----------|
| Spack snapshot   | T1-T5, T7                   | All      |
| Spack (no GPU CI) | T1-T4, T7                  | All      |
| UV layering base | T8.1-T8.5                   | All      |
| Serving profile  | T8.6 (vLLM), T8.8 (sglang) | Serving  |
| datasets         | T8.7                        | Non-block |

** Infra Verification Pipeline (=--infra= / =--infra-full= flags)

The full infra pipeline is orchestrated by =validate_all.sh= and chains four sequential stage scripts:

| Step | Script                  | What it does                                                       |
|------+-------------------------+--------------------------------------------------------------------|
|    0 | =verify_preflight.sh=   | Docker check, storage layout, host GPU (=diagnose_nvidia.sh=),    |
|      |                         | container GPU check (=verify-gpu.sh= entrypoint)                   |
|    1 | =verify_image_build.sh= | Build/verify base Docker image (auto/always/never policy)         |
|    2 | =verify_build.sh=       | Run Spack build job and wait for completion                       |
|    3 | =verify_epilogue.sh=    | ML validation (torch/jax), uv install test, UV-Spack provenance   |
|      |                         | (=verify_uv_spack.py=), job status/log checks                     |

=--infra= runs steps 0-1 (smoke).  =--infra-full= runs all four steps.

Note: =container/verify_all.sh= was deleted in R3 (2026-03-06); stages are now
invoked directly by =validate_all.sh=.

** Anti-Drift Contract Checks

=tools/check_zephyr_contracts.sh= validates that runtime behavior matches documented
contracts.  Invoked automatically by =validate_all.sh=.

** Validation Runbook

*** Developer pre-commit (fast, no Docker)

#+begin_example
./validate_all.sh --quick           # Go + Python lint (~15s)
./validate_all.sh --multi-repo-unit # Launcher unit tests (~5s)
#+end_example

*** Post image change (Docker, no GPU)

#+begin_example
./validate_all.sh --snapshot-no-gpu    # T1-T4, T7
./validate_all.sh --uv-layering-no-gpu # T8.1-T8.4
#+end_example

*** Full GPU validation

#+begin_example
./validate_all.sh --snapshot         # T1-T7 (snapshot image, GPU)
./validate_all.sh --uv-layering      # T8.1-T8.5 (UV layering, GPU)
./validate_all.sh --multi-repo       # Multi-repo unit + integration + E2E
#+end_example

*** Full infrastructure validation (Spack build + ML + uv)

#+begin_example
./validate_all.sh --infra            # Preflight + image only (~smoke)
./validate_all.sh --infra-full       # All stages: build + install + GPU + uv (30+ min)
#+end_example

* Cross-cutting concerns
:PROPERTIES:
:CUSTOM_ID: cross-cutting
:END:

** Security

*** Container Security

- *Dynamic user mapping*: Container user =kvothe= matches exact host UID/GID.
  Automatic conflict resolution if UID/GID already exists in image.
- *Minimal attack surface*: Ubuntu 24.04 LTS base, headless, no GUI components.
- *Network*: Host network access only when required (=--net=host=).
- *Resource limits*: CPU/memory constraints available via Docker.
- *Non-root execution*: =sudo= available for development convenience only.

*** Dependency Security

- *Trusted sources*: Official Spack packages and verified repositories.
- *Isolation*: Sandboxed builds prevent supply chain attacks.
- *Reproducible builds*: Lockfile-pinned installs (=spack.lock=) for cryptographic verification.

*** Access Control

- *Volume mounts*: Read-only mounts where possible (e.g., =/opt/sygaldry= in multi-repo mode).
- *Secrets management*: Environment variables and mounted files; never baked into images.
- *GPU access*: Controlled NVIDIA runtime exposure; no CPU-only mode.
- *Builder role gate*: Only =SYGALDRY_BUILD_ROLE=builder= can mutate Spack store.

** Operational

*** Prerequisites

| Resource | Minimum                 | Recommended  |
|----------+-------------------------+--------------|
| CPU      | 4 cores                 | 8+ cores     |
| RAM      | 16 GB                   | 32 GB        |
| Storage  | 50 GB                   | 100 GB+      |
| GPU      | NVIDIA (any)            | NVIDIA (any) |
| Software | Docker + NVIDIA runtime | Same         |

*** Timing Expectations

| Operation              | Duration                            |
|------------------------+-------------------------------------|
| Initial image build    | 15–25 min                           |
| Spack env from scratch | 30–60 min (3-8 hr for full rebuild) |
| Subsequent launch      | <10 s                               |
| Image rebuild          | 5–15 min                            |
| Cold CI build          | 60–120 min                          |
| Warm CI build          | 5–15 min                            |
| Incremental build      | 10 s – 5 min                        |

*** Performance Targets

- Cache hit ratio target: >80% for incremental builds.
- Container launch: <10 seconds.
- Spack unified concretization prevents host contamination and version conflicts.

** Human and agent ergonomics

- *Human*: One-line commands (Spack build, uv install, HF download); clear defaults; minimal env vars to start.
- *Agent*: Structured JSONL logs and status files for low-token monitoring; deterministic paths for introspection.
- *Job runner*: =tools/zephyr_job=; status under =outputs/.run-metadata/<run_id>/=; logs =<job>-<timestamp>.jsonl=.

** Container image

=dev_container.dockerfile=: Ubuntu 24.04, CUDA 12.9.1+cuDNN, dynamic user (host UID/GID), Python 3.13, Rust, Go, Bazel 6.4.0.  Conflict resolution for existing UID/GID; non-root execution with sudo for development.

* Host Storage Layout
:PROPERTIES:
:CUSTOM_ID: storage
:END:

** Zephyr Unified Layout

#+begin_example
/mnt/data_infra/zephyr_container_infra/              ($ZEPHYR_CACHE_ROOT)
├── shared/                                           ($ZEPHYR_SHARED_ROOT)
│   ├── hf_cache/           → /opt/hf_cache          HuggingFace models/datasets
│   ├── uv_cache/           → /opt/uv_cache          UV download cache
│   ├── bazel_cache/        → /opt/bazel_cache        Bazel build cache
│   ├── torch_cache/        → /opt/torch_cache        torch.hub downloads
│   ├── triton_cache/       → /opt/triton_cache       Triton kernel cache
│   ├── nv_compute_cache/   → /opt/nv_compute_cache   NVIDIA compute cache
│   └── jax_cache/          → /opt/jax_cache          JAX compilation cache
├── sygaldry/                                         ($ZEPHYR_BUILD_ROOT)
│   └── spack_store/        → /opt/spack_store        42 GB, builder-only mount
├── projects/                                         ($ZEPHYR_PROJECTS_ROOT)
│   └── <project_id>/                                 ($ZEPHYR_PROJECT_ROOT)
│       ├── home/             → /home/kvothe          User home (bash history, etc.)
│       ├── config/           → /home/kvothe/.config  XDG config
│       ├── local_share/      → /home/kvothe/.local/share  XDG data
│       ├── outputs/          → /workspace/outputs    Job outputs, staging results
│       ├── workspace/        → /workspace            Persistent workspace (multi-repo)
│       ├── runs/                                     Per-run raw logs
│       ├── leases/                                   GPU advisory leases
│       └── logs/                                     Per-project logs
└── meta/
    └── layout_version.json                           {"layout_version":2,...}
#+end_example

** Cache Sharing vs Isolation

| Cache       | Scope        | Why shared/isolated                                 |
|-------------+--------------+-----------------------------------------------------|
| =hf_cache=    | All projects | Models are large; avoid duplicate downloads         |
| =uv_cache=    | All projects | Package wheels are identical across projects        |
| =spack_store= | Builder only | Consumers use baked images                          |
| =torch_cache= | All projects | Pre-trained model weights                           |
| =home/=       | Per-project  | Bash history, SSH keys, git config vary per project |
| =outputs/=    | Per-project  | Job results should not cross-contaminate            |

** Concurrency: Lease Model

The launcher writes advisory GPU leases to prevent concurrent GPU-intensive
operations from interfering with each other:

#+begin_example
# Lease file: leases/gpu-all.lease
resource=gpu-all
owner=sygaldry
run_id=run-20260216-143022-12345
pid=12345
created_epoch=1739712622
expires_epoch=1739734222    # 6-hour TTL
#+end_example

Lease behavior controlled by =--lease-mode=:
- =off=: ignore leases
- =warn=: print warning if lease exists, continue
- =enforce=: fail if active lease exists

* Launcher Contract
:PROPERTIES:
:CUSTOM_ID: launcher
:END:

=container/launch_container.sh= is the single entry point for all container
operations.  It handles image building, volume mounting, environment injection,
entrypoint resolution, and lease management.

** Host-Side Configuration Knobs

These environment variables configure the launcher itself (set on the host before
invocation):

| Variable             | Default                                    | Purpose                               |
|----------------------+--------------------------------------------+---------------------------------------|
| =ZEPHYR_CACHE_ROOT=    | =/mnt/data_infra/zephyr_container_infra=     | Top-level storage root                |
| =ZEPHYR_SHARED_ROOT=   | =$ZEPHYR_CACHE_ROOT/shared=                  | Shared caches (HF, UV, etc.)          |
| =ZEPHYR_BUILD_ROOT=    | =$ZEPHYR_CACHE_ROOT/sygaldry=                | Builder Spack store root              |
| =ZEPHYR_PROJECTS_ROOT= | =$ZEPHYR_CACHE_ROOT/projects=                | Per-project roots                     |
| =ZEPHYR_PROJECT_ROOT=  | =$ZEPHYR_PROJECTS_ROOT/$SYGALDRY_PROJECT_ID= | Current project root                  |
| =SYGALDRY_RUN_ID=      | Auto-generated =run-YYYYMMDD-HHMMSS-PID=     | Per-run identifier                    |
| =ZEPHYR_LEASE_MODE=    | =warn=                                       | GPU lease: =off/warn/enforce=           |
| =ZEPHYR_CACHE_PROFILE= | =shared=                                     | Cache sharing: =shared/isolated/hybrid= |

*** Backward Compatibility

Legacy overrides are still honored with deprecation warnings:
- =SYGALDRY_SPACK_STORE= → overrides Spack store mount
- =SYGALDRY_HF_CACHE= → overrides HF cache mount
- =SYGALDRY_UV_CACHE= → overrides UV cache mount

** CLI Flags

| Flag                                   | Purpose                                 |
|----------------------------------------+-----------------------------------------|
| =--repo <path>=                          | Enable multi-repo mode                  |
| =--entrypoint <name>=                    | Select entrypoint (default: =default.sh=) |
| =--run-id <id>=                          | Set explicit run ID                     |
| =--lease-mode off/warn/enforce=          | Override lease behavior                 |
| =--cache-profile shared/isolated/hybrid= | Override cache sharing mode             |
| =--print-effective-config=               | Print resolved config and exit          |

** Control Flow

#+begin_example
1. Parse arguments (--repo, --entrypoint, --run-id, --lease-mode, etc.)
2. Determine mode: legacy (no --repo) or multi-repo (--repo <path>)
3. Warn on deprecated env vars (SYGALDRY_SPACK_STORE, etc.)
4. Check requirements: Docker daemon, NVIDIA runtime
5. Setup host directories: create all directories in the storage layout
6. Build container image (auto/always/never policy)
7. Detect baked Spack (docker inspect for sygaldry.spack.baked label)
8. Resolve mount paths (realpath all host directories)
9. Resolve entrypoint path (baked vs host-mounted)
10. Acquire GPU lease (if lease mode != off)
11. Build docker run args:
    ├── --rm --init [--interactive --tty]
    ├── --net=host --ipc=host
    ├── --user=<host-uid>:<host-gid>
    ├── Volume mounts (home, spack, caches, etc.)
    ├── --runtime=nvidia --gpus=all
    ├── Environment variables (30+ injected)
    └── --entrypoint=<resolved-path>
12. docker run $ARGS $IMAGE $PASSTHROUGH_ARGS
13. Release GPU lease
14. Exit with container's exit code
#+end_example

** Environment Variables Injected into Container

| Variable                  | Value                           | Purpose                     |
|---------------------------+---------------------------------+-----------------------------|
| =SYGALDRY_IN_CONTAINER=     | =1=                               | Container detection flag    |
| =SYGALDRY_PROJECT_ID=       | Project name                    | Project isolation namespace |
| =SYGALDRY_RUN_ID=           | =run-YYYYMMDD-HHMMSS-PID=         | Per-run identifier          |
| =SYGALDRY_ROOT=             | =/workspace= or =/workspace/<name>= | Sygaldry repo root          |
| =ZEPHYR_LEASE_MODE=         | =off/warn/enforce=                | Lease behavior              |
| =ZEPHYR_CACHE_PROFILE=      | =shared/isolated/hybrid=          | Cache sharing mode          |
| =HF_HOME=                   | =/opt/hf_cache=                   | HuggingFace cache           |
| =UV_CACHE_DIR=              | =/opt/uv_cache=                   | UV download cache           |
| =TORCH_HOME=                | =/opt/torch_cache=                | PyTorch hub cache           |
| =TRITON_CACHE_DIR=          | =/opt/triton_cache=               | Triton kernel cache         |
| =CUDA_CACHE_PATH=           | =/opt/nv_compute_cache=           | NVIDIA compute cache        |
| =JAX_COMPILATION_CACHE_DIR= | =/opt/jax_cache=                  | JAX compilation cache       |
| =XDG_CONFIG_HOME=           | =/home/kvothe/.config=            | XDG config directory        |
| =XDG_DATA_HOME=             | =/home/kvothe/.local/share=       | XDG data directory          |
| =XDG_CACHE_HOME=            | =/opt/uv_cache=                   | XDG cache directory         |

** GPU Validation

The launcher performs two GPU checks:

1. =docker info | grep nvidia= -- confirms NVIDIA runtime is installed
2. Host CUDA version >= =SYGALDRY_REQUIRED_CUDA_VERSION= (default 12.9)

If either fails, the launcher exits with an error.  There is no CPU-only mode.

** Concurrency Model

Run isolation is =project_id + run_id=.

- Per-run metadata and logs are stored under project outputs:
  =projects/<id>/outputs/.run-metadata/<run_id>/=
- The launcher creates an advisory GPU lease (=leases/gpu-all.lease=) with TTL.
- Lease behavior is controlled by =ZEPHYR_LEASE_MODE= (see Host-Side Configuration Knobs).

** Job Runner (=tools/zephyr_job=)

The host-side job runner manages container jobs with structured logging:

#+begin_example
tools/zephyr_job run    --project-id <id> --job <name> -- <command>
tools/zephyr_job status --project-id <id> --job <name>
tools/zephyr_job tail   --project-id <id> --job <name> [--lines N]
tools/zephyr_job stop   --project-id <id> --job <name>
tools/zephyr_job health --project-id <id> --job <name>
#+end_example

Storage paths:
- Raw logs: =projects/<id>/runs/<run_id>/raw/=
- JSONL + status: =projects/<id>/outputs/.run-metadata/<run_id>/<job>.status=
- JSONL logs: =projects/<id>/outputs/.run-metadata/<run_id>/<job>-<timestamp>.jsonl=

The job runner forwards =SYGALDRY_RUN_ID= and =ZEPHYR_LEASE_MODE= to the launcher.
This enables frequent, low-cost status polling without streaming full logs.

* Skill Distribution and Vendoring
:PROPERTIES:
:CUSTOM_ID: skill-distribution
:END:

Sygaldry exposes its container infrastructure to external repositories through a
vendored skill kit.  The skill kit is small (~5 files), self-contained, and
versioned independently of the Sygaldry monorepo.  Target repos never need a
Sygaldry clone.

** SD.1 Install into Target Repo

#+begin_example
# Infrastructure vendoring (required for jobctl, repoctl, container launch)
zephyr_vendor_infra.sh install \
    --target-repo <path> --snapshot-ref <digest-ref> [--force]

# MLSys overlay (optional; adds HF/vLLM/sglang profile launch)
skills/zephyr/scripts/zephyr_mlsys_vendor.sh install \
    --target-repo <path> --snapshot-ref <digest-ref> [--force]
#+end_example

The install script:
- Creates =.zephyr-mlsys/= (or updates it) in the target repo
- Writes =.zephyr-mlsys/bin/= with the portable launcher and job runner
- Records the pinned =snapshot-ref= for hermetic validation
- Migrates =.codex-zephyr-mlsys/= to =.zephyr-mlsys/= if the legacy directory exists

** SD.2 Validation Commands

#+begin_example
# Show effective configuration for a repo
repoctl config show --repo <path>

# Verify the pinned snapshot image is reachable and passes smoke tests
repoctl verify image --repo <path> [--skip-spack]

# Verify Spack view integrity inside the image
repoctl verify spack --repo <path>

# Verify UV+Spack layering (T8.1-T8.5)
repoctl verify uv-layering --repo <path>
#+end_example

** SD.3 Job Execution

The =jobctl= CLI manages container jobs from the host:

#+begin_example
jobctl run    --project-id <id> --job <name> -- <command>
jobctl status --project-id <id> --job <name>
jobctl health --project-id <id> --job <name>
jobctl tail   --project-id <id> --job <name> [--lines N]
jobctl stop   --project-id <id> --job <name>
#+end_example

Jobs produce JSONL logs and status files under:
=projects/<id>/outputs/.run-metadata/<run_id>/=

** SD.4 MLSys Overlay Launch

The portable kit in =.zephyr-mlsys/= provides profile-based container launch:

#+begin_example
# HuggingFace Transformers profile
.zephyr-mlsys/bin/launch-mlsys.sh hf-transformers [--no-validate]

# vLLM serving profile
.zephyr-mlsys/bin/launch-mlsys.sh vllm [--no-validate]

# HuggingFace Datasets profile (CPU-only test)
MLSYS_DISABLE_GPU=1 .zephyr-mlsys/bin/launch-mlsys.sh hf-datasets --no-validate
#+end_example

Each profile activates the corresponding UV venv and sets =HF_HOME=, =UV_CACHE_DIR=,
and related env vars.

** SD.5 Hermetic Burn-In Suite

The burn-in suite validates concurrent isolation, GPU utilization, and
framework correctness at a given snapshot reference:

#+begin_example
skills/zephyr/scripts/validate_hermetic_runtime_suite.sh \
    --snapshot-ref <digest> --mode burnin --burnin-iterations 5
#+end_example

Expected outcomes per iteration:
- Concurrent run isolation (no cross-contamination of outputs)
- PyTorch NCCL multi-GPU DDP
- JAX =pmap= across GPUs
- LLVM/Numba JIT compilation and execution
- CUDA kernel launch

** SD.6 Packaging Gates

Skill packages are deterministic archives with provenance:

#+begin_example
# Package infrastructure skill
skills/zephyr/scripts/package_infra.sh \
    --version <ver> --out-dir <dir> --snapshot-ref <ref> [--smoke-mode full|skip]

# Package MLSys overlay skill
skills/zephyr/scripts/package_mlsys.sh \
    --version <ver> --out-dir <dir> --snapshot-ref <ref> [--smoke-mode full|skip]
#+end_example

Outputs per package:
- =<name>-<ver>.tar.gz= — deterministic archive
- =<name>-<ver>.SHA256SUMS= — SHA-256 checksum
- =<name>-<ver>.provenance.json= — build metadata (timestamp, git ref, snapshot digest)

** SD.7 Validation Snapshot Reference

Current pinned snapshot digest (update when image is rebuilt and validated):

#+begin_example
ghcr.io/phi9t/sygaldry/zephyr:spack@sha256:8c9507aea53995f29a5712c0cbdb99deb3d571fb9631b3d42352b3d6d6fb668c
#+end_example

This digest is the reference for all =--snapshot-ref= flags in burn-in and
packaging commands.  When updating, run the full T1-T8.5 test suite and update
this section.

