# Local Skills

This file is generated by `skills/local-skills-sync/scripts/sync_local_skills.py`.
Do not edit manually; rerun the sync script.

This repo contains **repo-scoped** skills under `skills/` for validation before global install.

## codex-dev-loop (local only)

Run a structured two-agent development loop where Codex implements changes and an outer reviewer critiques and requests revisions before testing. Use when you want repeatable implement-review-test cycles with JSONL logs and bounded revision/test-fix attempts.

Paths:
- Skill root: `skills/codex-dev-loop/`
- SKILL.md: `skills/codex-dev-loop/SKILL.md`
- Scripts: `skills/codex-dev-loop/scripts/`
- Agent metadata: `skills/codex-dev-loop/agents/openai.yaml`

Do not install into `$CODEX_HOME/skills` until validation is complete.

## codex-headless (local only)

Run OpenAI Codex CLI in headless (`exec`) mode for non-interactive code generation, review, and editing. Use when you need scripted Codex runs, reproducible CLI workflows, or JSON output without interactive chat.

Paths:
- Skill root: `skills/codex-headless/`
- SKILL.md: `skills/codex-headless/SKILL.md`
- Scripts: `skills/codex-headless/scripts/`
- Agent metadata: `skills/codex-headless/agents/openai.yaml`

Do not install into `$CODEX_HOME/skills` until validation is complete.

## local-skills-sync (local only)

Keep the repo-scoped skill index synchronized with the filesystem by generating and validating `skills/LOCAL_SKILLS.md` from actual `skills/*/SKILL.md` directories. Use when adding/removing/renaming local skills, reviewing skill metadata drift, or preparing commits that touch `skills/`.

Paths:
- Skill root: `skills/local-skills-sync/`
- SKILL.md: `skills/local-skills-sync/SKILL.md`
- Scripts: `skills/local-skills-sync/scripts/`
- Agent metadata: `skills/local-skills-sync/agents/openai.yaml`

Do not install into `$CODEX_HOME/skills` until validation is complete.

## nvidia-container-troubleshooting (local only)

Diagnose and fix NVIDIA GPU access issues in Docker/OCI containers (NVML initialization failures, missing /dev/nvidia* nodes, misconfigured NVIDIA container runtime/CTK, cgroup v2 device rules, CDI device injection, or driver/library mismatches). Use when CUDA containers cannot see GPUs, `nvidia-smi` fails inside a container, or `--gpus` does not work.

Paths:
- Skill root: `skills/nvidia-container-troubleshooting/`
- SKILL.md: `skills/nvidia-container-troubleshooting/SKILL.md`
- References: `skills/nvidia-container-troubleshooting/references/`
- Agent metadata: `skills/nvidia-container-troubleshooting/agents/openai.yaml`

Do not install into `$CODEX_HOME/skills` until validation is complete.

## zephyr (local only)

Run GPU container jobs with Spack (PyTorch/JAX), build and validate

Paths:
- Skill root: `skills/zephyr/`
- SKILL.md: `skills/zephyr/SKILL.md`
- Validation: `skills/zephyr/VALIDATION.md`
- Scripts: `skills/zephyr/scripts/`
- References: `skills/zephyr/references/`
- Assets: `skills/zephyr/assets/`
- Environment definitions: `skills/zephyr/envs/`
- Portable kit: `skills/zephyr/portable/`
- Agent metadata: `skills/zephyr/agents/openai.yaml`

Do not install into `$CODEX_HOME/skills` until validation is complete.
