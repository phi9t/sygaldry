# Canonical list of pip package names managed by Spack.
#
# This file is the single source of truth for packages that UV must NOT
# rebuild or override.  Both configure_uv_spack() (bashrc) and
# uv-install.sh read this file to populate UV_NO_BUILD_ISOLATION_PACKAGE
# and generate Spack constraint files.
#
# Derived from pkg/zephyr/spack_src.yaml.
# Format: one pip package name per line (blank lines and # comments ignored).

# Deep Learning Frameworks
torch
torchvision
torchaudio
jax
jaxlib
triton

# Scientific Computing
numpy
scipy
scikit-learn
numba
llvmlite

# Data Science and Visualization
matplotlib
pandas

# Audio
soundfile

# Interactive
jupyterlab

# Build-isolation exceptions (not Spack-owned, but need system torch at build time)
transformer-engine-torch
